{
  "name": "@nova/nova-vectorstore-sdk",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "lint": "uv run --package nova-vectorstore-sdk ruff check . && uv run --package nova-vectorstore-sdk mypy src",
    "test": "uv run --package nova-vectorstore-sdk pytest",
    "build": "echo 'nova-vectorstore-sdk: no build step (pure Python library)'"
  }
}
