{
  "name": "@nova/nova-contracts",
  "private": true,
  "version": "0.1.0",
  "main": "typescript/index.ts",
  "scripts": {
    "lint": "uv run --package nova-contracts ruff check . && uv run --package nova-contracts mypy src",
    "test": "uv run --package nova-contracts pytest",
    "build": "uv run --package nova-contracts python codegen/generate_typescript.py"
  },
  "devDependencies": {
    "json-schema-to-typescript": "^15.0.4"
  }
}
