"""In-memory fakes for every `domain.ports` Protocol -- used by `tests/unit`
and (task #67) `tests/contract`/`tests/integration` alike, the same
fakes-substitution pattern every Phase 1/2A engine's own test suite uses."""
