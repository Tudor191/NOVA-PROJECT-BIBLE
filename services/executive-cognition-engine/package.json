{
  "name": "@nova/executive-cognition-engine",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "lint": "uv run --package executive-cognition-engine ruff check . && uv run --package executive-cognition-engine mypy src",
    "test": "uv run --package executive-cognition-engine pytest",
    "build": "echo 'executive-cognition-engine: no build step (container image built via Dockerfile in CI)'",
    "dev": "uv run --package executive-cognition-engine uvicorn nova_executive_cognition_engine.main:app --reload --port 8000"
  }
}
