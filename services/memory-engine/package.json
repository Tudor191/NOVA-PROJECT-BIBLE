{
  "name": "@nova/memory-engine",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "lint": "uv run --package memory-engine ruff check . && uv run --package memory-engine mypy src",
    "test": "uv run --package memory-engine pytest -m \"not real_infra\" --cov=nova_memory_engine.domain",
    "build": "echo 'memory-engine: no build step (container image built via Dockerfile in CI)'",
    "dev": "uv run --package memory-engine uvicorn nova_memory_engine.main:app --reload --port 8000"
  }
}
