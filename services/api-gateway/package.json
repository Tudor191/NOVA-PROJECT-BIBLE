{
  "name": "@nova/api-gateway",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "lint": "uv run --package api-gateway ruff check . && uv run --package api-gateway mypy src",
    "test": "uv run --package api-gateway pytest",
    "build": "echo 'api-gateway: no build step (container image built via Dockerfile in CI)'",
    "dev": "uv run --package api-gateway uvicorn nova_api_gateway.main:app --reload --port 8000"
  }
}
