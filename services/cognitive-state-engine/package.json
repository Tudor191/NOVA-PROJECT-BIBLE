{
  "name": "@nova/cognitive-state-engine",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "lint": "uv run --package cognitive-state-engine ruff check . && uv run --package cognitive-state-engine mypy src",
    "test": "uv run --package cognitive-state-engine pytest -m \"not real_infra\" --cov=nova_cognitive_state_engine.domain",
    "build": "echo 'cognitive-state-engine: no build step (container image built via Dockerfile in CI)'",
    "dev": "uv run --package cognitive-state-engine uvicorn nova_cognitive_state_engine.main:app --reload --port 8000"
  }
}
