# Phase 4B — Project Health Snapshot

Source: the [Phase 4B Gate Review](../roadmap/architecture-reviews/phase-4b-observability-panels-gate-review.md)
(2026-09-06), which is repository-driven — every figure in it was produced by a
command run in the session that wrote it, against head
`0f3412c73b6dbd28b48c6f1ff9de14db3c238e53`. Every field below cites the section
of that document it comes from. Fields with no available value read **"Not
reported"** or **"Not measured"** with the reason, per [`README.md`](README.md)'s
rule; nothing is inferred from an adjacent phase or backfilled from today's tree.

| # | Field | Value |
|---|---|---|
| 1 | Phase identity | Phase 4B |
| 2 | Phase/Sub-Phase name | Observability panels — the six panels [`00-master-scope.md`](../design/phase-4/00-master-scope.md) §6 assigns to 4B: **Planning**, **Reasoning Trace**, **Capabilities**, **Approvals**, **Events**, and **Health** (`system/` in §6's table) — plus the `api-gateway` route-table and `ws-gateway` allow-list widening they require, two new list endpoints, and two new outbox-dispatch worker services. Decision **D-8** (build all six rather than a subset) is the approved scope |
| 3 | Report date | 2026-09-06 |
| 4 | Branch | `phase-4b`, branched from the merged `phase-4` head `481ceac` (the PR #23 merge commit). 5 commits. Working tree clean, nothing unpushed at verification time. `main` (`7e273e6`), `phase-4` (`481ceac`) and `phase-4a` (`fbd75d7`) all untouched — confirmed against remote refs. No force push, no rebase, no squash. (Gate Review §11) |
| 5 | PR number | **#24**, `phase-4b` → `phase-4`, opened 2026-09-06 at the user's explicit request so CI could execute the Docker-backed verification that cannot run in the development container. **Open and NOT merged.** Protocol §11.1's "no PR unless the user asks" condition is satisfied by that explicit instruction. (§11) |
| 6 | Commit / merge commit | **Verified code SHA `26eb4f46070404cf4d7eb30f15553093332ec9a5`** — the G-8 arq worker-isolation fix, the last commit to change production code. *(Was `2f0414a` for the second pass, `0f3412c` for the first.)* Later commits change tests, CI evidence steps and documentation only: `e841618` corrected the new isolation test's own over-specified assertion and added `workflow_dispatch`. Third-pass commit chain: `26eb4f4` the fix · `e841618` test assertion + manual trigger · this documentation pass. **Prior text, preserved:** *Verified code SHA `2f0414a338fd6f18afd261c1b223246de7fb5b55`* *(was `0f3412c` for the first pass; the second pass built DEV-1/2/3 and re-verified against this head)* — every figure in fields 12–16 was produced against it, and all three CI workflows ran against it. Commits after it change documentation only. **No branch head is named here by design**, following [`phase-3e.md`](phase-3e.md) field 6's precedent: each documentation commit would falsify it. Use `git rev-parse HEAD`. Commit chain: `9af0708` gateway plumbing · `b10e30d` the two list endpoints · `a7cf674` the six panels · `b22ec83` E2E correction (Capabilities has rows) · `0f3412c` E2E correction (panels settle). **Not merged**, so there is no merge commit |
| 7 | Phase status | **Implementation complete and engineering-verified; gate verdict CONDITIONAL-GO** (Gate Review §15.1, third pass 2026-09-06). **The user explicitly approved deferring AC-3's approval-execution clause**, recorded under protocol §2.4 as **"Deferred by approval (approval cited)"** (Gate Review §0.3, §9.2). **AC-3 is still not reported as met**: 1 of 4 sub-clauses met, 1 deferred by that approval, 2 unverifiable without a provider — §2.1's "partially met is not met" is not softened. The deferral's stated reason is **not** a Phase 4D dependency: master scope §1.1 says AC-1…AC-4 depend on no new engine. It is the **Phase 3D / ADR-032** configurable-threshold mechanism that was never built (**CF-9**), routed forward to 4D's policy surface. No threshold was invented, no zero-confidence policy seeded, `perception-engine` not added to force the clause green, fail-closed behaviour unchanged. **Both blockers are discharged** — B-2 by the work, B-1 by the approval — and seven conditions C-1…C-7 replace them. **G-8 found and closed**: the 4A golden path's intermittent CI failure was a real transport defect, not flakiness (Gate Review §4.1). *(Read the assessment below through the first two passes.)* **Was: NO-GO** (Gate Review §15). All six panels built and browser-verified. **Acceptance criteria: 0 of 1 Met** — AC-3 remains **Not met**, but its composition changed after the second pass (Gate Review §9.1): **1 of 4 sub-clauses now met** (a capability is installed, from the browser, verified live), **1 blocked** by ADR-032's fail-closed identity gate denying every Critical Action at stage 3 so the approval loop is never reached (G-7), and **2 unverifiable** without an LLM provider. **Blocker B-2 is discharged** — DEV-1, DEV-2 and DEV-3 are built and verified against the real stack. **B-1 stands.** **NO-GO is a statement about approved scope and acceptance criteria, not about engineering quality** — every engineering gate is green. The verdict converts to **CONDITIONAL-GO** with six conditions if the user explicitly approves deferring AC-3 and ratifies the three narrowings (§15) |
| 8 | Production SLOC | **Not measured.** Neither `cloc` nor `scc` is installed in this environment (`command -v cloc` and `command -v scc` both empty — Gate Review §16). Per [`README.md`](README.md) this reads "Not measured" and is never estimated, inferred, or backfilled; no substitute line count is offered, because a hand-rolled count would not be comparable to either historical series. The last measured figure remains Phase 3E's **31,319** (`cloc` v1.98, comparable scope) |
| 9 | Total SLOC | **Not measured** — same reason as field 8 |
| 10 | SLOC methodology/tool | **None applied this phase.** Phase 3E's `cloc` v1.98 measurement is the most recent in the series; 4B adds no new measurement, so the series is unbroken rather than extended. The **Option A / Option B methodology decision recorded in [`project-health-master.md`](project-health-master.md) §2 remains open and is not decided here** |
| 11 | SLOC scope | **N/A this phase** — nothing measured. 4B introduced no new top-level directory, so whichever scope is eventually chosen remains applicable unchanged |
| 12 | Test status | **Third pass (head `26eb4f4`+): `pnpm turbo run test --force` 30/30 successful, 0 cached — 1,992 passing, 107 deselected; `uv run pytest tools/tests -q` 179 passed; `pnpm turbo run lint --force` 30/30; `pnpm turbo run typecheck --force` 5/5; `uv run lint-imports` 7 kept / 0 broken; `uv sync --all-packages --frozen` clean. Grand total 2,171 passing, 0 failing** — exactly +44 on the second pass's 2,127 (11 derivation unit tests, 33 repository-level isolation guards), plus 3 new `real_infra` tests bringing deselected from 104 to 107. Coverage unchanged: the G-8 fix touches worker wiring, not any `domain/` package. (Gate Review §6.1) **Second pass, preserved:** `pnpm turbo run test --force` (**uncached**, 0 of 30 tasks cached): **30/30 successful**, **1,813 pytest passed + 146 vitest passed**, 104 deselected (`real_infra`), 0 failures. `pnpm turbo run lint --force`: **30/30 successful, 0 cached**. Separately `uv run pytest tools/tests -q`: **146 passed**. **Grand total 2,127 passing, 0 failing** after the second pass added 22 frontend tests (was 2,105). (Gate Review §6) |
| 13 | Test count | **Third pass added 47 tests: 11 + 33 + 3.** `packages/nova-service-kit/tests/test_worker.py` **11** — the per-service arq derivation, checked against arq's real `CronJob` and real name derivation, including that the schedule is forwarded untouched and that a caller-supplied `name=` is refused. `tools/tests/test_worker_queue_isolation.py` **33** — every `services/*/workers/__init__.py` parsed with `ast` (never imported: importing one constructs that engine's `Settings()`), asserting each declares `_SERVICE_NAME`, sets `queue_name = worker_queue_name(_SERVICE_NAME)`, routes every cron through `service_cron`, and that no two workers share a service name or a derived cron identity. `packages/nova-service-kit/tests/test_worker_real_redis.py` **3 `real_infra`** — real arq workers on a real Redis: no cross-queue consumption, the un-namespaced `arq:job:` guard swallowing a colliding enqueue **even across distinct queues**, and the shared-default-queue theft reproduced as the negative control. **Negative-controlled**: reverting `perception-engine` to a bare `cron(...)` with no `queue_name` failed both guards by name, 2 failed / 31 passed; restored 33/33. *Prior passes, preserved:* 4B added **30 new frontend tests** (16 reducer in `observability-reducers.test.ts`, 14 panel in `observability-panels.test.tsx`) plus a **4-spec Playwright E2E spec** (`observability-panels.spec.ts`) kept separate from the 4A golden path so AC-1 keeps failing on its own terms. Per package at verification: `web-client` 117, `nova-ui` 29, `api-gateway` 72, `ws-gateway` 64 (+8 for the corrected `fnmatch` topic guard and its parametrised SDK-parity twin), `planning-engine` 138, `action-engine` 66, `nova-contracts` 113, `tools` 146. The live Docker E2E ran **8 specs, 0 failures** — the four 4A golden-path specs plus the four 4B panel specs |
| 14 | Coverage | Against the 85% `fail_under` gate, per affected package: `api-gateway` **98%** (109 stmts / 2 miss), `ws-gateway` **98%** (66/1), `planning-engine` **99%** (286/2), `action-engine` **97%** (255/8). No affected package is below the gate. `apps/web-client` and `packages/nova-ui` are **not coverage-gated** — the 85% gate is `[tool.coverage.report]` in `pyproject.toml` and applies to Python domain packages only. (§6) |
| 15 | CI status | **GREEN — 30/30 Check Runs `completed`/`success` against head SHA `2f0414a`** (2026-09-06, PR #24, second pass): PR Checks [34043818680](https://github.com/Tudor191/NOVA-PROJECT-BIBLE/actions/runs/34043818680) (#76), Build & Scan [34043818694](https://github.com/Tudor191/NOVA-PROJECT-BIBLE/actions/runs/34043818694) (#76), Real-Infrastructure Checks [34043818681](https://github.com/Tudor191/NOVA-PROJECT-BIBLE/actions/runs/34043818681) (#105), all `success`. The live E2E ran **13 specs: 12 passed, 1 skipped, 0 failed** — the skip is the approval spec, which skips itself when no undecided approval exists (G-7). Runs #73-#75 are retained as history: #73/#74 red on a Playwright strict-mode violation in a new spec ("Uninstall" contains "Install") and a driver step that could not run (no `uv` in that job), #75 red on the approval spec alone. **The 4A golden path went red in #73/#74 and green again in #75/#76 with no change touching it — transient, not a regression.** First-pass evidence, retained: **30/30 against `0f3412c`** (2026-09-06): **PR Checks** run [34023254683](https://github.com/Tudor191/NOVA-PROJECT-BIBLE/actions/runs/34023254683) (#71) success, both jobs; **Build & Scan** run [34023254685](https://github.com/Tudor191/NOVA-PROJECT-BIBLE/actions/runs/34023254685) (#71) success; **Real-Infrastructure Checks** run [34023254679](https://github.com/Tudor191/NOVA-PROJECT-BIBLE/actions/runs/34023254679) (#100) success, all 11 matrix jobs. Zero failed, cancelled, timed out or skipped. Two earlier runs on this branch are retained as history: **#69** against `a7cf674` and **#70** against `b22ec83` each failed on **one Playwright assertion**, both defects in the new E2E spec rather than in the panels, corrected by `b22ec83` and `0f3412c` respectively; their `checks`, Build & Scan and Real-Infra jobs were green throughout. **Verify via the Check Runs API, not the legacy commit-status endpoint** — the latter returns `{"state":"pending","total_count":0}` for this repository because it publishes no legacy statuses at all, exactly as [`phase-3e.md`](phase-3e.md) field 15 records. (§11) |
| 16 | Real-infrastructure status | **Docker locally: NOT available** (`docker info` non-zero) — testcontainers cannot run in this environment. **The CI path did run and pass against this exact head**: `real-infra-checks.yml` run [34023254679](https://github.com/Tudor191/NOVA-PROJECT-BIBLE/actions/runs/34023254679), **all 11 matrix jobs success** against `0f3412c`. Separately, the **PR Checks Playwright job executed the full real Docker stack**: 34 compose services started (86s), schema migration confirmed, engines confirmed not crash-looping, both gateways answered, 8 E2E specs passed. **Deselected `real_infra` tests in the packages 4B changed, enumerated by name in Gate Review §8**: `api-gateway` **none** (no `real_infra` tests at all), `ws-gateway` **none**, `planning-engine` **15**, `action-engine` **12** — all 27 in each engine's `tests/integration/test_repository_real_postgres.py`. **The finding that matters: 4B added exactly two repository methods, `PostgresPlanningRepository.list_all` and `PostgresActionRepository.list_pending_approvals`, and neither appears in either enumerated list.** 4B's own new SQL — including its `ORDER BY created_at DESC` — has **no dedicated real-Postgres test in any environment**; it is covered by in-memory fakes plus the live E2E stack, which reads it through the browser but asserts only that the panel settled without a degradation notice. The 11 `nova-testkit` fixture tests (Redis/Neo4j/NATS) remain unrun locally and touch no code 4B changed. (§8) |
| 17 | Documentation health | **Third pass changed six documents** (2026-09-06), every edit additive and dated: this record (fields 6, 7, 12, 13, 15, 17, 18, 20, 21, 23), the Gate Review (new §0.3 the approved G-7 deferral, §0.4 and §4.1 G-8, §6.1 verification, §7.1 the E2E series, §9.2 AC-3 after the deferral, §13.2 G-7 decided and G-8 closed, §15.1 the CONDITIONAL-GO verdict), `project-health-master.md`'s §1 row, `ENGINEERING_ROADMAP.md`'s 4B row — **which was stale and contradicted the repository**, still saying "three approved capabilities were not built" after the second pass had built them, a §12 contradiction corrected here — `00-master-scope.md` §4's new **CF-9**, and an additive **implementation-status note on ADR-032** recording that its decision point 2 has no implementation. The ADR's decision itself is untouched. *Prior passes, preserved.* **Ten documents changed by this closure pass** (Gate Review §14): this record, the Gate Review, `project-health-master.md` (§1 row, §3 index entries for both `phase-3e.md` — which was **missing** — and `phase-4b.md`, plus removal of a stray blank line that had silently split §1 into two markdown tables), the `ENGINEERING_ROADMAP.md` Phase 4 entry (additive status block), `docs/architecture/11-api-architecture.md` §2, all four affected subsystem READMEs, and an additive 4B status note in `00-master-scope.md`. **Both gateway READMEs were unfilled scaffold stubs carrying `TODO:` placeholders** — the PHR-3 defect class the 2026-08-29 Project Health Review named — and 4B had changed both their public surfaces; both are now written. **The top-level `README.md` is deliberately unchanged**: its Status section describes `main`, where Phase 4 is not merged, and it makes no claim about Phase 4 that 4B falsifies. Documents inspected and found already accurate are listed in §14 as evidence the sweep was not selective. **A repository-wide sweep found zero documents claiming Phase 4A or 4B is complete, closed, merged or GO** (`grep -rniE 'phase 4[ab][^\|]{0,60}(complete\|closed\|merged\|go\b\|gate review)' docs/ README.md` → 0 hits), so no phase-status contradiction exists anywhere |
| 18 | Architecture status | Every 4B architectural rule verified held (Gate Review §1, §12): browser never connects to NATS (asserted by test — a direct socket to `:4222` is refused); browser never calls engines; `api-gateway` sole external REST surface, five allow-listed prefixes forwarded verbatim per **D-6**; `ws-gateway` sole realtime bridge per ADR-006 / doc 09 §6; `/internal/*` unreachable, asserted byte-for-byte identical to an unrouted path; one envelope `{data, meta, error}`, no second shape; no polling for realtime state; no optimistic mutation of shared cognitive state. `uv run lint-imports`: **7 kept, 0 broken** — unchanged, 4B added no package. **No ADR falsified and no new ADR required**; 4B added no event subject, no RPC, no table and no migration. **A 4A guard defect was found and corrected**: `test_every_public_topic_is_reachable_on_the_bus` used `topic in SUBSCRIBABLE_SUBJECTS or f"{first}.*" in ...`, which silently passes any subject containing `*`; `BoundEventBus` authorises with `fnmatchcase`, where `*` spans dots, so the guard now uses `fnmatchcase` and a parametrised test pins SDK parity. **Five deviations registered** (§2.1). Three were undisclosed narrowings and are now **built and closed** (Gate Review §0.1): DEV-1 install/uninstall through the gateway to `capability-engine`'s existing endpoints, no backend change needed; DEV-2 filtering by subject and `correlation_id`, applied at render so the feed keeps recording through a filter; DEV-3 depth derived from `ReasoningTrace.steps` and shown beside `reasoning_level` rather than instead of it, with no contract change and no invented field. **DEV-4** (`GET /v1/system/health` unbuilt, Health push-fed) and **DEV-5** (`useLiveProcesses` uses `enabled: false` rather than `skipToken`) remain disclosed and open |
| 19 | Security status | **Third pass: unchanged, and deliberately so.** The G-8 fix touches internal arq queue naming only — no event subject, payload, REST path, WebSocket frame, table or migration. **`PUBLIC_TOPICS` is unchanged**, `api-gateway`'s five-prefix allow-list is unchanged, and `apps/web-client/src/` is untouched; the `/internal` indistinguishability and refused-direct-NATS-socket specs pass unmodified. **On G-7 specifically: no security control was weakened to close it.** No `IdentityConfidencePolicy` row was seeded, no threshold chosen, no zero-confidence policy created, and `perception-engine` was not added to the E2E stack to manufacture a passing identity signal. ADR-032's fail-closed default — absent policy requires confidence 1.0 — is byte-identical to what it was. *Prior text follows.* The two browser-facing boundaries are asserted by test against the live stack, not by inspection: **`/internal/*` is indistinguishable from an unrouted path** (status, content-type and body all byte-for-byte identical, and positively not JSON), and **a direct browser WebSocket to NATS `:4222` is refused**. `api-gateway`'s route table is an allow-list of five prefixes, not a pattern (**D-6**), so widening it cannot accidentally expose an unintended path; `executive-cognition-engine` and `nova-core` are deliberately not fronted. `ws-gateway`'s `PUBLIC_TOPICS` is a fixed bounded list of 17, not a policy engine — full permission-derived allow-lists depend on Phase 7's `nova-auth` and remain an explicit Phase 4 non-goal. Session model is ADR-025's single-trusted-user-per-instance with an httpOnly cookie (**D-3**), unchanged by 4B. **Trivy: no result required** — 4B touched no Dockerfile; `build-and-scan` nonetheless ran its full 14-service matrix green against `0f3412c` |
| 20 | Unverified infrastructure | **Third-pass update (2026-09-06).** **(f) NEW, and CLOSED in the same pass — G-8.** All ten engine workers shared one Redis, arq's global `arq:queue`, and a cron job named `cron:arq_run_outbox_dispatch`, so at each 10-second tick exactly one job existed and one randomly-chosen worker drained only its own outbox. `communication-engine`'s outbox was dispatched on ~1 tick in 4; run #77's diagnostics show **0 of 14 rows dispatched** while every turn was persisted and NOVA's reply produced. A pre-existing Phase 2/3 defect, unobservable while the E2E stack ran a single arq worker and exposed when 4B took it to four; also a production defect. Fixed in `26eb4f4` (Gate Review §4.1, §6.1). **A new `if: always()` CI step now reports every engine's outbox on green runs as well as red and asserts no `communication.turn.received` row is stranded** — the absence of exactly that check is how this survived nine runs. **(b) re-verified, not assumed.** Because the collision stranded *every* engine's outbox, (b)'s attribution to the absent `ANTHROPIC_API_KEY` was re-checked rather than carried forward: it stands, because with no provider nothing produces a completed or failed reasoning process to write an outbox row for, so there was no row to strand. The collision was a second, independent reason the same subjects could not have been delivered. *Original text follows.* Four named gaps, none closed by this phase. **(a)** `list_all` and `list_pending_approvals` have **no real-Postgres test** (field 16) — the same defect class that let Phase 3E's D-1 and D-2 survive undetected from Phase 2B. **(b)** **`reasoning.process.*` and `ai_model.model.*` carried no observed frame.** Both subjects are authorised (negative-controlled topic guard) and both owning workers — `reasoning-engine-worker`, `ai-model-orchestration-engine-worker` — start in the real stack and tear down cleanly by name. But no test observed a frame on either, because `ANTHROPIC_API_KEY` is unset in the E2E stack (`docker-compose.local.yml:340,408`) and nothing produces one. **Verified as reachable, not as delivered.** **(c)** `GET /v1/system/health` **does not exist**; doc 11 §2 names it and master scope §5 lists it as a 4B backend addition, but `nova-core` exposes only `/internal/*`, which doc 11 §3 makes permanently unroutable, so the Health panel is push-fed instead. **(d) NEW — the approval loop is unreachable in this stack.** ADR-032's stage-3 identity gate requires confidence 1.0 when no `IdentityConfidencePolicy` row exists, and observes 0.0 with no perception activity, so every Critical-risk Action is denied at Check Permissions and never writes a `pending_approval` row (Gate Review §0.2, G-7). The gate is working as designed; seeding a zero-threshold policy to make the E2E green would have weakened a security control, so it was reported rather than done. **(e)** SAD 15 §9.1 items **2, 3 and 7** — sequence diagrams, component diagrams, performance benchmarks — are **absent** for the extended surface (§14.1). Pre-existing and unaffected: `agent-os` still has no Dockerfile (Phase 3E's ratified deferred obligation) |
| 21 | Open blockers | **None. Both blockers are discharged** (Gate Review §15.1, third pass 2026-09-06). **B-2** was discharged by building DEV-1/2/3. **B-1** is discharged by the user's **explicit approval, 2026-09-06, to defer AC-3's approval-execution clause** — recorded under protocol §2.4 as "Deferred by approval (approval cited)" in Gate Review §0.3 and §9.2. **That is a deferral, not a pass: AC-3 remains not met**, and nothing in this record says otherwise. Seven conditions replace the blockers: **C-1** AC-3's approval clause and the CF-9 mechanism reassigned to Phase 4D · **C-2** the two provider-dependent sub-clauses demonstrated once a provider exists · **C-3** a `real_infra` test for `list_all`/`list_pending_approvals` · **C-4** SLOC measured before `phase-4` merges to `main` · **C-5** SAD 15 §9.1 items 2, 3, 7 supplied or waived · **C-6** Phase 4A's Gate Review and health record written before `phase-4` closes · **C-7** the E2E job promoted to a required check only once its stability series is judged sufficient — branch protection is **not** changed here. **G-8 was opened and closed within this pass** and required no user decision: no contract, threshold or security control is involved. *Prior text, preserved.* **One, requiring a user decision** (Gate Review §15, as re-confirmed after the second pass). **B-2 is discharged.** *(Originally two.)* **B-1 — AC-3 is unmet.** "Every Phase 3 sub-phase 3A–3D is exercised end-to-end **from the browser**: a plan is generated and rendered, a reasoning trace is inspected, a capability is installed, and a risky action is blocked pending approval and then approved." All four sub-clauses fail: two because generation needs an LLM provider the phase does not add, two because the UI to do them was not built. Protocol §2.1 — partially met is not met. **B-2 — DEV-1/2/3 are undisclosed narrowings** of approved scope; protocol principle 0.3.6 forbids narrowing scope unilaterally. Clearing either requires the user's explicit approval to defer/ratify, or the work itself. **Also recorded, and explicitly NOT blockers on 4B: Phase 4A has no Gate Review and no Project Health record.** Gate Review §13.1 determines from the protocol that this does not gate 4B — §3.2's GO condition 9 concerns the phase being gated, §0.2's ledger rule concerns a Sub-Phase's own Slices rather than a sibling milestone, and a repository-wide sweep found zero documents claiming 4A is complete or closed, so no §12 contradiction exists. Phase 4A is left entirely untouched; the gap is carried here so `phase-4`'s eventual closure inherits it. Pre-existing and unaffected: the SLOC methodology Option A/B decision in [`project-health-master.md`](project-health-master.md) §2 |
| 22 | Branch hygiene | `phase-4b` is pushed and current with `origin`; last production-source commit `a7cf674`, last verified commit `0f3412c`; working tree clean; no unpushed commits. Correctly branched from the **merged** `phase-4` head `481ceac`, per master scope §16 rule 5 ("every milestone branches from the freshly merged `phase-4` HEAD"). **PR #24 open, not merged.** `main` `7e273e6`, `phase-4` `481ceac` and `phase-4a` `fbd75d7` verified untouched against remote refs. `phase-4a` is preserved and is a git ancestor of `phase-4`. No branch was deleted (no branch deletion is authorized). **`phase-4c` deliberately not created** — master scope §16 rule 4 creates milestone branches strictly one at a time, and completing this record is not authorization to begin 4C |
| 23 | Notes / important findings | **Third pass: the second most important finding was one CI reported nine times and nobody read.** The 4A golden path had been failing intermittently since 4B — including on `85f7682`, a **documentation-only** commit, where `2f0414a` had passed with byte-identical application code. It was called flakiness twice in this record's own history and left alone. It was not flakiness: **all ten engine workers shared one Redis, one arq queue and one cron job name**, so `communication-engine`'s outbox was dispatched on ~1 tick in 4 and nothing the browser could do would make the turn appear. The evidence had been printed by the job's own `Trace the reply through the transport` step every time it failed — *0 dispatched, 14 undispatched* — and the diagnosis needed nobody's Docker, only reading it. **The failing test was right and the transport was wrong**, and the fix changed no assertion, no timeout, no retry count and nothing under `apps/web-client/src/`. **The generalisable lesson, now enforced rather than remembered:** the outbox check ran `on failure()` only, so a green run examined nothing — and the defect's whole shape was that green runs were the lucky quarter. That step now runs `if: always()`. **Also third pass:** G-7 was decided by the user as a **deferral**, and the record deliberately keeps three facts apart that are easy to collapse into one — the mechanism's **original owner is Phase 3D / ADR-032**, the **discovering phase is 4B**, and **Phase 4D is where it is routed**, which is not the same as AC-3 depending on 4D (master scope §1.1 says it does not). No identity-confidence threshold was invented and the fail-closed gate is untouched. *Prior passes, preserved.* **The most important finding of this pass was invisible to CI.** 30 of 30 Check Runs were green, including a full Docker-backed E2E, while the milestone's sole acceptance criterion was unmet and three approved capabilities were unbuilt — because no test asserts either. That is precisely the failure mode this protocol's category 2 exists to catch, and it was caught only by enumerating acceptance criteria from the design documents rather than inferring health from a green pipeline. **A prior report in this same session stated the engineering verdict was "effectively GO"; that assessment did not enumerate AC-3, and this record supersedes it.** **Two E2E defects were found and fixed by CI, both in the new test rather than in the panels.** Run #69: the spec asserted all four REST panels render `panel-empty`, but `capability-engine` bootstrap-installs four built-ins through its real 8-stage pipeline before reporting ready (`main.py:191`), so a *healthy* stack always has capabilities — the assertion was wrong in the direction that matters, passing on a broken bootstrap and failing on a working one. Run #70: `panel-empty` was a proxy for the property, not the property; the test now asserts each panel **settles** (`panel-loading` gone within 15s, because a panel still reading has not reached its engine either) and does not settle on a degradation notice, with the panel named in every assertion. **Comparing runs #70 and #71 shows one of Planning / Reasoning / Approvals held at least one row on the live stack; which one is not established, and is deliberately no longer asserted** — how much data a panel holds is a fact about how busy the system is on the day, and pinning it is what made the test wrong twice. **Also found: both gateway READMEs were unfilled scaffold stubs** (`TODO:` placeholders) for the two components 4A built and 4B extended, **One correction to this pass's own first attempt**, recorded rather than silently reverted: the four READMEs were initially changed to say none of these services exposes `GET /internal/metrics`, on the strength of `api/health.py` alone. All four do — `main.py` mounts `prometheus_asgi_app()` there as a sub-application — so the claim was false and has been corrected back. **Also found: `project-health-master.md` §3's index was missing `phase-3e.md`** while a stray blank line had silently split its §1 timeline into two markdown tables — the second rendering without a header |

---

## Relationship to the Phase 4 series

Phase 4B is the second of Phase 4's six milestones (4A–4F) and the one master
scope §5 calls *"the milestone that makes Phase 3 visible."* It is the first point
at which `planning-engine`, `reasoning-engine`, `capability-engine` and
`action-engine` — built across Phases 3A–3D and until now reachable only from
other engines — are readable from a browser through the approved boundaries.

It does not, however, satisfy the criterion that measures that visibility. AC-3
asks for those four engines to be *exercised* from the browser, not merely
*observed* from it, and 4B built the observation surface without the two controls
(capability install, an end-to-end approval) and without the provider that
generation requires. The distinction between surfacing Phase 3 and exercising it
is the whole of this milestone's NO-GO.

**Phase 4A's own Gate Review and Project Health record do not exist.** That gap is
recorded here (field 21) and analysed against the protocol in the Gate Review
§13.1, which finds it does not gate 4B. It is carried forward so `phase-4`'s
eventual closure inherits a complete list rather than rediscovering it — the same
discipline [`README.md`](README.md) and protocol §0.2 apply to Slice ledgers.

**Completing this record is not authorization to begin Phase 4C, and not
authorization to merge PR #24.**
