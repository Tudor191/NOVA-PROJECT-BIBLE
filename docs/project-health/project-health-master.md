# NOVA Project Health — Master Index

The longitudinal, chronological record of every Phase/Sub-Phase's project-health data, built exclusively from what each phase's own Gate Review, Architecture Review, or the one-time Project Health Review actually reported. **No value in this document is calculated, estimated, or backfilled from the current repository state unless explicitly labeled as such.** Where a source document does not report a field, this document says **"Not reported"** — it does not guess, and it does not average a value in from adjacent phases.

See `README.md` in this directory for what this system is, how it differs from a Gate Review, and how to update it going forward.

---

## 1. Master timeline

| Phase | Status | Production SLOC | Total SLOC | SLOC Methodology | Tests | Coverage | CI | Real Infra | Documentation |
|---|---|---|---|---|---|---|---|---|---|
| [Phase 1](phase-1.md) | Go | 9,794 | 30,946 | `cloc --skip-uniqueness` | 376 | 79% | Pass (local) | Not exercised (no Docker) | Healthy, 1 stale artifact fixed in-review |
| [Phase 2A](phase-2a.md) | Go | 12,412 | 38,541 | `cloc` (no `--skip-uniqueness` — later flagged as not comparable) | 480 | NR (aggregate); 84% new engine only | Pass (local) | Not exercised | Healthy, 1 stale artifact fixed in-review |
| [Phase 2B](phase-2b.md) | Go | 15,326 | 45,544 | `cloc --skip-uniqueness` (corrected back) | 558 | 80.3% | Pass (local) | Partial — first-ever native-Postgres round trip, ad hoc | Healthy |
| [Phase 2C](phase-2c.md) | Go | 17,116 | 51,763 | `cloc --skip-uniqueness` | 637 | 80.7% | Pass (local) | Partial — deeper native-Postgres round trip, ad hoc | Healthy, 1 self-reported arithmetic discrepancy (101 vs 102) |
| [Phase 2D-A](phase-2d-a.md) | Go (1 item flagged for explicit user decision) | 20,969 | 61,387 | `cloc --skip-uniqueness` | 804 | 79.0% | Pass (local) | Not exercised | Healthy |
| [Phase 2D-B](phase-2d-b.md) | Go | 31,610 | 77,286 | **`scc`** (tool changed from `cloc`) | 951 | 78.7% | Pass (local) | Not exercised, 3-engine backlog | Healthy — crossed the 30,000 SLOC Project Health Review threshold |
| [Project Health Review (2026-08)](../roadmap/architecture-reviews/project-health-review-2026-08.md) | Proceed to 2D-C, after a small cleanup pass | 31,610 (re-confirmed unchanged) | 77,939 | `scc` | N/A (review only) | N/A | N/A | N/A | Deep one-time review — 1 High, several Medium/Low findings (see §2) |
| [Phase 2D-C](phase-2d-c.md) | Gate passed, Option B, explicitly disclosed as incomplete | 33,175 | 85,141 | `scc` (claimed "consistent with every prior gate review") | 1,008 | 99.37% (communication-engine negative control) | Pass (local only, no GH Actions run cited) | Written, not executable (no Docker) | Healthy, scope explicitly bounded by the user's own instruction |
| [Phase 2D-C Closure — Priority 5](phase-2d-c-closure.md) | Go | 24,959 (narrower scope — **not comparable**, see §2) | NR | **`cloc`** (tool changed again, silently, contradicting 2D-C's own consistency claim) | 18/18 packages pass | 99.22% / 100% (`style_selector.py`) | Pass (local only) | N/A (no schema/DB change) | 1 unrelated documentation drift noted, explicitly not fixed |
| [Phase 2D-D](phase-2d-d.md) | Complete | NR (no SLOC section) | NR | NR | 1,179 passed, 45 deselected | 98.70% / 98.85% (negative controls) | Pass (local) | **Fully real-infra confirmed**, 2 real GitHub Actions runs, 1 real bug found and fixed | Healthy |
| [Phase 3A](phase-3a.md) | Complete | NR (delta only: +160) | NR | NR | 87 | 94% | Pass (local; monorepo 19/19, 1185/1185) | N/A, correctly (no new persistence/subject) | Healthy |
| [Phase 3B — Domain Foundation (PR #2)](phase-3b.md) | Complete, 1 real bug + 1 coverage-claim error fixed by independent post-review pass | NR | NR | NR | 35 | 100% (corrected from an inaccurate original 100% claim) | `checks` pass; `build-and-scan` pre-existing unrelated failure | N/A (no persistence yet) | 1 disclosed deviation from TDD 3B's own proposal |
| [Phase 3B — Decomposition Orchestration (PR #7)](phase-3b.md) | Complete | NR | NR | NR | 57 | 99% branch | 3/3 workflows green, real GitHub Actions (18 check runs) | Partial — real NATS JetStream round trip explicitly unverified | **Documentation-sync gap identified**: its own cited research (`phase-3b-decomposition-research`, commit `654570d`) remains unmerged into the canonical lineage as of this record |
| [Phase 3B — Planning Persistence (`phase-3b-planning-persistence`, PR #18, merged)](phase-3b.md) | Complete, merged into canonical `phase-3b-planning-domain` 2026-08-20 (squash SHA `a72bd83f`) | NR | NR | NR | 67 passed, 10 real-infra deselected (77 total) — independently re-run against the merged canonical tree | 98.68% domain | 24/24 checks green, real GitHub Actions, confirmed against the PR's fully-tested head `dc89dda` (zero tree diff from the merge commit `a72bd83f`) | Fully real-infra confirmed via CI: 10/10 real-Postgres tests passed against a real `postgres:16-alpine` container (first-ever `planning-engine` real-infra matrix entry); one genuine defect (unstringified `depends_on` UUIDs) caught by the first push and fixed same-day; fix independently re-confirmed present on canonical post-merge | Closes TDD 3B §4/§5/§6.2 — TDD 3B status banner and both prior Phase 3B Gate Reviews additively updated; dated Phase 3E research reconciliation note added and updated with merge confirmation, no Phase 3E decision reopened |
| [Phase 3C](phase-3c.md) | Complete, merged (PR #8) | NR in the gate review itself | NR in the gate review itself | NR in the gate review itself | 59 passed, 6 deselected | 97% | 20/20 green, real GitHub Actions, re-confirmed live post-merge | Fully real-infra confirmed after a mid-review CI-matrix fix | Resolved — `phase-3c-research`'s decisions synced into canonical TDD 3C via PR #11 (2026-08-17) |
| [Phase 3D](phase-3d.md) | **Go** — all 7 of TDD 3D §14's acceptance criteria met (deep `Capability.input_schema` validation at stage 5, the last gap, closed 2026-08-18 by PR #13 commit `046d459`); CI 22/22 green; **merged into `phase-3b-planning-domain` 2026-08-18** as squash commit `ac285bc3533fb24d0434d7675b8fc3af2db1d079` — see the [Gate Review](../roadmap/architecture-reviews/phase-3d-action-engine-gate-review.md) | NR | NR | NR | 62 passed, 8 deselected (action-engine, up from 47 after the deep-validation commit); capability-engine/nova-contracts unaffected, re-verified 61 passed / 86 passed | 97% (action-engine domain, incl. 100% on the new `parameter_validation.py`); capability-engine domain unchanged at 97% | 22/22 green, real GitHub Actions, re-confirmed live at head `046d459` (2026-08-18) | Fully real-infra confirmed via CI (8/8 real-Postgres tests, Docker build + Trivy scan) | Two additive TDD 3D §14 notes landed (2026-08-18): one disclosing the deep-validation gap, one recording its same-day closure; Gate Review updated twice the same day (Conditional → Go); `phase-3d-research`/PR #12 itself deliberately not synced into canonical lineage until both implementation PRs merged, per the same sequencing precedent PR #11 established (research syncs after its implementation PR merges, not before). **Update (2026-08-18):** PR #14 also merged the same day (merge commit `e9ea3b8c6ae99c645e6eb41b98fbe34c55f8ec39`), satisfying that precedent's condition. **Update (2026-08-19):** the sync itself is now complete — PR #15 (`phase-3d-research-sync`) merged as commit `bf748d0ef4157da3a60e0eb880a24a58264d9387`; all Phase 3D-relevant documentation (implementation, closure, and research) is now fully in canonical lineage |
| [Phase 3E](phase-3e.md) | **GO (2026-08-30), merged** — implementation complete on `phase-3e-agent-os` (verified code SHA `60934ac`); **PR #20 merged and closed 2026-08-30** into canonical `phase-3b-planning-domain` as **true merge commit `59743423f32b3b8f8c470128b30cf4b798b1f46f`** — a merge commit, not a squash, so all 27 Phase 3E commits remain ancestors of canonical; acceptance criteria 3 of 5 Met outright + 2 Met with disclosed narrowing, 0 Not Met (unchanged by the CI pass). **All 6 conditions closed**: the closure pass 2026-08-29 closed C-2…C-6, and the CI pass 2026-08-30 discharged **C-1** — see the [Gate Review](../roadmap/architecture-reviews/phase-3e-agent-os-gate-review.md) §10.1 and §15.1 | **31,319** (comparable scope) / **34,446** (full, incl. `agent-os/*` + `agents/*`) | 120,266 (excl. generated TS) / 122,151 (incl.) | **`cloc` v1.98** — continues the 2026-08 audit's `cloc` measurement, **not comparable** to the `scc` series (§2); the Option A/B decision in §2 remains open | 1,646 passed, 104 deselected (`pnpm turbo run test --force`, uncached, 52/52 tasks, 0 cached), plus 73 Agent Package tests, now gated by `pr-checks.yml`'s per-package `agents/*` loop (C-2) | kernel 98%, registry 99%, supervisors 96%, planning 99%, reasoning 94%, action 97%, capability 97%; gate 85%; no package below it | **GREEN — 27/27 Check Runs `completed`/`success` against the final head SHA `258ebe6`** (PR #20, 2026-08-30): PR Checks 33326242931, Build & Scan 33326242949, Real-Infrastructure Checks 33326242919, all three `success`; an earlier, equally green 27/27 run against `733a31d` (33305319352 / 33305319349 / 33305319396) is the original C-1 discharge evidence. **The merge commit `59743423f32b3b8f8c470128b30cf4b798b1f46f` was not itself CI-executed** — no workflow triggers on `phase-3b-planning-domain`; zero failed/cancelled/timed-out/skipped/running. Verify via the Check Runs API — the legacy commit-status endpoint returns `total_count: 0` for this repo and its `pending` is an empty-list default, not a pending check. *(Read "NONE — zero runs exist" through 2026-08-29; the local-only figures in the adjacent columns are unchanged and were produced before CI ran.)* | **59 `real_infra` tests executed and passed against a real, locally-installed PostgreSQL 16.13** (kernel 7 incl. the full acceptance E2E, registry 13, planning 15, reasoning 6, action 12, capability 6) — Docker unavailable **locally**, so those figures came from a locally-installed server; **the testcontainers path CI uses has since run and passed** — run 33305319396, all 11 matrix jobs success against `733a31d` (2026-08-30). 11 `nova-testkit` fixture tests still unrun locally | 15 files updated across the Gate Review and closure passes, plus a pre-PR audit pass; **6 TDD deviations found, 3 previously undisclosed anywhere, all now RATIFIED as Phase 3E narrowings** (C-4 closed); `agents/*` CI gate implemented and negative-controlled (C-2 closed); `agent-os` deployment ratified as a **deferred obligation** with a criterion-by-criterion justification (C-3 closed); one stale source docstring corrected; **decisions D1–D4 and D10–D12 verified absent by a second independent method and were not invented** (C-6 closed); closes Phase 3B's last deferred item |
| [Phase 4B](phase-4b.md) | **CONDITIONAL-GO (2026-09-07, final review), not merged.** A second explicit user approval (2026-09-07) defers AC-3's two provider-dependent sub-clauses, so **all eleven of protocol §3.2's GO conditions now hold** — condition 1 accepts a criterion *"narrowed/deferred with a cited user approval"*. **The verdict is still CONDITIONAL-GO, not GO**, because four conditions remain open; Phase 3E reached GO only once all six of its conditions were closed. **AC-3 remains NOT MET** — 1 of 4 sub-clauses met, **3 deferred by approval**. **C-3, C-4, C-5 CLOSED. C-1, C-2, C-6 carried forward with explicit user authorization. C-7 OPEN at 8/10** — PR Checks #78→#85 all green; the last two runs are unobtainable (`403` on both dispatch and re-run) and none was fabricated. **No security control weakened**: no `IdentityConfidencePolicy` seeded, ADR-032 unmodified in substance, fail-closed default byte-identical. PR #24 passes all six `definition-of-done.md` item-10 merge-readiness checks; **the protocol contains no clause authorizing merge under CONDITIONAL-GO**, so the merge decision is the user's. *(Read the text below through the earlier passes.)* **Was: CONDITIONAL-GO (2026-09-06, third pass), not merged.** Both blockers discharged: **B-2** by building DEV-1/2/3, **B-1** by the user's **explicit approval to defer AC-3's approval-execution clause**, recorded under protocol §2.4 as "Deferred by approval (approval cited)". **AC-3 is still not met (0 of 1)** — 1 of 4 sub-clauses met, 1 deferred by that approval, 2 unverifiable without a provider; §2.1's "partially met is not met" is not softened. The deferral is **not** a Phase 4D dependency (master scope §1.1: AC-1…AC-4 depend on no new engine) but the **Phase 3D / ADR-032** configurable-threshold mechanism that was never built (**CF-9**), routed forward to 4D's policy surface. **No threshold invented, fail-closed gate unchanged.** Seven conditions C-1…C-7 replace the blockers. **G-8 found and closed:** the 4A golden path's intermittent CI failure was a real transport defect — every engine worker sharing one arq queue and one cron job name, stranding `communication-engine`'s outbox on ~3 ticks in 4 — fixed in `26eb4f4`, with the golden path's assertions, timeout, retries and selectors unchanged and `apps/web-client/src/` untouched. **Verified code SHA `26eb4f4`.** *(Read "NO-GO (2026-09-06)" and the text below through the first two passes.)* **Was: NO-GO (2026-09-06), not merged** — six observability panels built and browser-verified against the real Docker stack. **Second pass built the three missing capabilities** (DEV-1 capability install/uninstall, DEV-2 filterable events, DEV-3 3A recursion depth), all verified live, discharging blocker B-2. **Acceptance criterion AC-3 remains unmet (0 of 1)**: of its four sub-clauses 1 is now met, 1 is blocked by ADR-032's fail-closed identity gate denying every Critical Action before the approval loop, and 2 need an LLM provider Phase 4 does not add. One blocker remains and needs a user decision. **NO-GO is about approved scope and acceptance criteria, not engineering quality** — every engineering gate is green. PR #24 (`phase-4b` → `phase-4`) open — see the [Gate Review](../roadmap/architecture-reviews/phase-4b-observability-panels-gate-review.md) §9 and §15 | **32,923** (comparable scope) / **36,050** (Phase 3E full scope) / **38,369** (full, incl. `apps/*/src`) — measured 2026-09-06 by the Conditional-GO closure pass (condition C-4); **+1,604 (+5.1%)** over Phase 3E on the comparable scope. *(Read "Not measured — neither `cloc` nor `scc` is installed" through the first three passes.)* | **135,118** (excl. generated TS) / **137,280** (incl.) | **`cloc` v2.06** — same tool and same scope definitions as Phase 3E, **different version** (2.06 vs 1.98), so recorded with the version attached and **not** claimed exact-to-the-line comparable (§2 item 12). `--skip-uniqueness` variant reported beside it (32,979). The `scc` series stays separate; the Option A/B decision in §2 **remains open** | 2,105 passed, 104 deselected (`pnpm turbo run test --force`, uncached, 30/30 tasks, 0 cached: 1,813 pytest + 146 vitest, plus 146 `tools/tests`), 0 failures; 30 new frontend tests; live Docker E2E 8 specs 0 failures | api-gateway 98%, ws-gateway 98%, planning 99%, action 97%; gate 85%; no affected package below it. `apps/web-client` and `packages/nova-ui` are not coverage-gated (the gate is Python-only) | **GREEN — 30/30 Check Runs `completed`/`success` against head SHA `0f3412c`** (PR #24, 2026-09-06): PR Checks [34023254683](https://github.com/Tudor191/NOVA-PROJECT-BIBLE/actions/runs/34023254683), Build & Scan [34023254685](https://github.com/Tudor191/NOVA-PROJECT-BIBLE/actions/runs/34023254685), Real-Infrastructure Checks [34023254679](https://github.com/Tudor191/NOVA-PROJECT-BIBLE/actions/runs/34023254679), all three `success`. Runs #69/#70 each failed one Playwright assertion — both defects in the new E2E spec, not the panels — fixed by `b22ec83` and `0f3412c`. Verify via the Check Runs API; the legacy commit-status endpoint returns `total_count: 0` for this repo | **CI path ran and passed**: `real-infra-checks` all 11 matrix jobs success against `0f3412c`; the PR Checks Playwright job started the full 34-service Docker stack (86s) and ran 8 E2E specs green. **Docker unavailable locally.** Deselected `real_infra` enumerated by name (§8): api-gateway 0, ws-gateway 0, planning 15, action 12 — **and neither of 4B's two new repository methods (`list_all`, `list_pending_approvals`) appears in either list**, so their SQL has no real-Postgres test in any environment | 10 documents updated; **zero documents anywhere claim Phase 4A or 4B complete/closed/merged/GO** (sweep, 0 hits), so no phase-status contradiction exists; both gateway READMEs were unfilled `TODO:` scaffold stubs and are now written; this index's §3 was missing `phase-3e.md` and a stray blank line had split §1 into two tables — both fixed. **Phase 4A still has no Gate Review and no Project Health record** — recorded, analysed in Gate Review §13.1, and found *not* to gate 4B |
| [Phase 4C](phase-4c.md) | **CONDITIONAL-GO (2026-09-10 Gate Review; §18 addendum 2026-09-11), MERGED 2026-09-12.** Phase 4C is exactly two milestones — **4C.1** `agent-os` containerization (PR #25, merged as `3433fbe`) and **4C.2** the Agents surface in seven slices 4C.2a–4C.2g (PR #26, merged as the two-parent merge commit `b1d7ca548e88ddc2fc8d5f56007a8864127cb798`, not squashed). **There is no 4C.3/4C.4/4C.5/4C.6** — a search across all branches and all history finds those three in zero commits ever, and `4C.3` only in the 4C.2 Gate Review's own placeholder sign-off phrase. **4C implementation is complete.** **C-1 DISCHARGED** — real-infra 12/12 green, kernel **50/50 in one pytest process** including the Phase 3E real-PostgreSQL acceptance E2E, registry 18, total 68; it did not discharge cleanly, exposing a real transaction-ordering defect (`4eafa80`, decision D-3 preserved) and a test-isolation defect (`388c271`, `list_instances()` unchanged), both fixed and verified. **C-2 DISCHARGED** — 34/34 Check Runs green against head `7360763`. **C-4 (documentation reconciliation) DISCHARGED.** **C-3 OPEN: AC-4 remains NOT MET** — clause 1 Met by 4C.1; clauses 2 and 3 *"renders live agent instances"* and *"at least one real peer-review round"* **Deferred by explicit user approval of 2026-09-07**, the remaining trigger being **provider configuration** and nothing else. §2.1's "partially met is not met" is unaltered. **The user explicitly authorized the merge under CONDITIONAL-GO because C-3 is an approved deferral; the verdict was not raised to GO.** `main` unchanged at `7e273e6`. Carried to Phase 4 closure: 4C.1 has no Gate Review, Phase 4A has neither, and `README.md` carries no Phase 4 status line | **32,948** (comparable scope) / **36,573** (Phase 3E full scope) / **39,258** (full, incl. `apps/*/src`) — measured 2026-09-10 at head `a405bca` (Gate Review §16); **+25** over Phase 4B on the comparable scope, because 4C.2's production code lands almost entirely in `agent-os/` and `apps/`, which that scope excludes. Re-measured on identical scope strings at the merged head: comparable reproduced **exactly**, production SLOC moved **+1 line** in total. **~50,000 gate NOT crossed** | **Not reported** — the Gate Review reports production scopes and the architecture/quality metric set, not a whole-repository total; not backfilled | **`cloc` v2.06** — same tool **and** same version as Phase 4B, so this figure continues that series comparably rather than only directionally. §2 needs no new entry: neither tool nor scope definitions changed. The `scc` series stays separate; the Option A/B decision in §2 **remains open** | **2,460 passing, 0 failing** (2,242 via `pnpm turbo run test --force`, 30/30 uncached, `Cached: 0`, + 218 `tools/tests`); 322 deselected. lint 30/30 · typecheck 5/5 · import-linter **7 kept, 0 broken** · codegen **116 files, zero drift** · compose valid. **20/20 negative controls fired**; **65 flakiness runs** clean across seven series | `agent-os/kernel` **99%** (295/4), `agent-os/registry` **99%** (206/2) against the 85% gate; no affected package below it. `api-gateway`, `ws-gateway` and `apps/web-client` are not coverage-gated (the gate is Python-only) | **GREEN — 34/34 Check Runs `success` against head `7360763`** (2026-09-12): `checks`, 12/12 `real-infra`, 19/19 `build-and-scan` incl. Trivy `CRITICAL,HIGH`, `dependency-audit`, and the staged non-blocking Playwright golden path. `build-and-scan` needed one re-run: attempt 1's `docker/setup-buildx-action@v3` failed pulling BuildKit from Docker Hub (`connection reset by peer`), fail-fast cancelling 18 siblings; the step record shows Build and Scan both **skipped**, so no repository file was read — infrastructural, not a repository result. Attempt 2 is Build + Trivy success on all 19 images. Verified via the **Check Runs API** | **PASSED — the field Phase 4B could not fill.** `real-infra-checks` all **12/12** matrix jobs success against `7360763`; kernel **50 passed, 190 deselected in a single pytest process**, registry **18**. Docker unavailable locally throughout, so every real-infrastructure result is CI's and is labelled as such; nothing simulated | 4C.2g changed eight documents plus the new Gate Review; the 2026-09-11 pass added the Gate Review's **append-only §18** (+189/−0, §§0–17 and the original Sign-off byte-identical) and corrected `ENGINEERING_ROADMAP.md`'s 4C row, which still read **"Not started"** — a direct contradiction of the repository. Sweep: **zero** documents claim 4C is "Not started", claim 4C.2 merged while it was not, or claim 4C/4C.2 is GO. No historical section rewritten; every superseded figure affirmed as correct for its own head, per protocol §0.3.4 |
| [Phase 4D](phase-4d.md) | **GO (2026-09-14, final Gate Review §22) — MERGED** into `phase-4` as **`68397a200f08db497fc4f36a1b14422dc953298e`**, a normal two-parent merge of PR #27 at head **`dff646cc0123b33a710e93b294e8db231a2dfe38`**. **All five conditions C-1…C-5 DISCHARGED**; CI **36/36 green at the exact head SHA**, zero failed, zero cancelled. **CF-9, CF-10 and CF-11 remain OPEN** — GO closes no carry-forward. C-4 was discharged by a documentation-only reconciliation of TDD §5.3/§4.1 (no Event Bus trust subject, no RPC, no architecture change); C-5 by PR #28, which fixed the stale `tools/scaffold-engine.py` Dockerfile template at its root. ***Superseded record, true at head `f5f263f`:*** **CONDITIONAL-GO (2026-09-13), NOT MERGED. CI-VERIFIED 2026-09-13** via PR #27, opened solely to obtain evidence; head then **`f5f263fc10e52c93bbf18e2dd1f37e1dcd4038f5`**. **C-1, C-2 and C-3 DISCHARGED** — 36 checks, **28 success**, and every check exercising 4D green: `checks`, `real-infra (autonomy-engine)` **16/16 twice**, `Playwright` with **both AC-5 specs passed in a real browser** (972 ms, 377 ms). The CI pass found three defects, all fixed without weakening anything: a Dockerfile missing its `nova-service-kit` COPY (which alone took down both the image build and the whole e2e stack), two of this milestone's own `real_infra` tests combining a sync `pytest.raises` with an `async with`, and 12 pre-existing Debian CVEs in `api-gateway`'s unpatched base. **C-4 OPEN** (TDD reconciliation) and **C-5 NEW/OPEN** — `build-and-scan (ws-gateway)` fails on the same pre-existing CVE set; `git diff origin/phase-4..HEAD -- services/ws-gateway/` is **0 lines**, and fixing it is outside this branch's ratified scope, so it is reported rather than fixed and needs a decision. Neither open condition is an approved deferral, so this must **not** be read as GO. The Autonomy Engine (Bible Part 14) in four slices on `phase-4d`, head `de6dbc979ada8b46b07de9bea195808f0a2a934d`. **Four open conditions, none an approved deferral:** **C-1** no workflow run exists at any SHA — both workflows trigger only on `pull_request` or a push to `main`, and no pull request was opened, so this is an *absence* of CI evidence and not a red result · **C-2** the 16-test `real_infra` tier has never executed (Docker unreachable locally) · **C-3** the AC-5 Playwright spec has never executed · **C-4** two TDD clauses need reconciling. **C-1 is the root of C-2 and C-3**, and one authorized action resolves all three. Eleven of twelve acceptance criteria fully Met; AC-5 Met at unit and integration tier, unexecuted in a browser. **Two TDD clauses were found to presuppose surfaces that do not exist and were named rather than built around** — structurally the same error D-4D-2 had already identified for CF-9: **CF-10** no read surface exists for 2D-D's `TrustMetric` (`digital-twin-engine` serves no trust subject and exposes no trust route, and `BoundEventBus.request()` gates on the allow-list D-4D-1 requires to stay empty) and **CF-11** no component produces a suggestion, so the inbox is permanently empty in production. Neither blocks AC-5; neither was resolved by inventing the missing surface. **CF-9 remains OPEN by ratified decision D-4D-2.** `action-engine`, `nova-contracts` and `ws-gateway` are untouched — verified by diff, 0 files and 0 lines respectively. `PUBLIC_TOPICS` byte-identical; both engine allow-lists empty; **0 new Event Bus subjects, 0 new dependencies**. `phase-4` and `main` untouched, `main` still `7e273e6` | **34,413** (comparable scope) / **39,754** (4C.2's wider scope) / **43,058** (full, incl. `apps/*/src`) — measured at head `de6dbc9`. Growth is reported against the branch base measured with **identical scope strings** (32,948 / 38,289 / 40,974), whose middle figure **reproduces the 4C.2 record exactly**: **+1,465 / +1,465 / +2,084**. **~50,000 gate NOT crossed** | **Not reported** — production scopes and the metric set only; not backfilled | **`cloc` v2.06** — same tool **and** version as Phase 4B and 4C.2, so the series continues comparably. §2 needs no new entry: neither tool nor scope definitions changed. The `scc` series stays separate; the Option A/B decision in §2 **remains open** | **2,715 passing, 0 failing** (2,491 via `npx turbo run test --force`, 31/31, `Cached: 0`, + 224 `tools/tests`). lint 31/31 · typecheck 5/5 · import-linter **7 kept, 0 broken** · codegen **116 files, zero drift** (4D adds no wire contract). **All 12 of TDD §16's negative controls demonstrated by removing the property and observing failure**; **20 flakiness runs** clean (10× engine, 10× panel). **All local — no CI** | `nova_autonomy_engine.domain` **99%** (350 stmts / 3 miss) against the 85% gate. `api-gateway` and `apps/web-client` are not coverage-gated (the gate is Python-only) | **RAN — 36 checks at `f5f263f`: 28 success, 1 failure, 7 cancelled.** The one failure is `build-and-scan (ws-gateway)` (C-5, pre-existing), whose fail-fast cancelled the seven. **No check exercising 4D failed.** `build-and-scan (autonomy-engine)` recorded Build **success** + Scan **success** at `32b6dcd`, image byte-identical at `f5f263f`. *(Originally "NONE — no workflow run exists", correct for head `5a2bf77`.)* | **PASSED — 16 passed, 222 deselected, 8.64 s**, green on two consecutive heads. Proves the four only-real-Postgres-provable properties TDD §16 names. It earned its place on the first run, which caught two failures in this milestone's own *test code* (not the implementation); the other 14 passed first time. Docker stayed unreachable locally, so every real-infrastructure result is CI's. *(Originally "NOT EXECUTED", correct for head `5a2bf77`.)* | Two new documents (the Gate Review and `phase-4d.md`) plus two reconciled living references. **No historical section rewritten.** The Gate Review **records rather than silently fixes** two TDD clauses the repository contradicts (§5.3's Event Bus trust read, §4.1's diagram edge — both residue from before D-4D-1 that its correction pass missed); reconciling them is offered for ratification as condition C-4 |
| [Phase 4E](phase-4e.md) | **GO (2026-09-15, Gate Review §15) — MERGED AND CLOSED.** PR **#29** (`phase-4e` → `phase-4`) **merged 2026-09-15T16:32:29Z** on explicit user authorization as the two-parent merge commit **`59bbeee3838290ab31b0627b64c6fc0b2bceb13b`** (parents `f39fa6c` and `df4606c`; no squash, no rebase, no force push). `phase-4e` is preserved at `df4606c`, not deleted, and is an ancestor of `phase-4`; all ten 4E commits remain reachable. `main` untouched at `7e273e6`. **No `phase-4f` branch exists and no Phase 4F work has started.** ***Superseded record, true at head `df4606c`:*** **NOT MERGED.** PR #29 is **open**, `mergeable_state: clean`, opened solely to obtain CI evidence ahead of the Gate Review with merge authorization explicitly withheld. ***End superseded text.*** **No conditions are attached to this GO.** **AC-6 is MET at four tiers** — unit, integration, real-Postgres and browser — and it is 4E's only acceptance criterion. Bible Part 16's **nine remaining Digital Twin domains** (eleven total, two shipped in 2D-D), derived from **existing** Event Bus subjects with **0 new subjects registered** (118 at base, 118 at head), four evidence states, six machine-readable reason codes, five REST routes behind one 1:1 D-6 gateway prefix, and a lazily-loaded panel. **The two controls that matter most are types, not tests:** `DomainModel`'s validators make *"populated with zero evidence rows"* and *"a non-populated state with no reason code"* **unconstructible**, and enforce the ratified per-domain 4F floor — a derivation bug raises rather than rendering a plausible number. **AC-6's "simulated multi-week gap" is simulated in the data, never in the clock:** real `created_at` values 118–37 days in the past, through the real `PostgresMemoryRepository` write path into real PostgreSQL, on the real transactional outbox, over the pre-existing `memory.long_term.created` subject; a repository-wide sweep for `freeze_time`/`freezegun`/`time_machine`/`FakeClock` returns **zero matches**. The enabling fix is two lines: `create_long_term` built its ORM row from nineteen fields and **omitted `created_at`/`updated_at`**, so both column defaults fired and the caller's timestamps were silently discarded while `_memory_to_domain()` read them back — one authority now, on insert and update alike, with **no migration** and **no Memory HTTP contract change**. **The milestone's most consequential finding is a deployment one:** because `memory.long_term.created` is not in `PUBLIC_TOPICS`, Phase 4A's stack guard exempted `memory-engine` from needing a worker, no compose service ran it, and therefore **no `memory.*` subject had ever been published in the local stack** — invisible until AC-6 needed it, and the cause of three Playwright 404s on the first CI run. Fixed by deploying the **existing** worker (`workers/` byte-identical to base) and by teaching the guard the **two-hop** shape structurally, with four anti-vacuity controls. **Three findings remain OPEN as explicitly non-blocking carry-forwards:** `nova_testkit`'s Postgres image drift (pre-existing), the D-6 prefix exposing six pre-4E operations that take a caller-supplied `user_id` (reported not fixed, pinned by two tests, an integrity and latent multi-user surface rather than a confidentiality one under ADR-025), and a stranded-outbox guard that does not yet watch the `memory` schema. **CF-9, CF-10 and CF-11 all remain OPEN — GO closes no carry-forward.** `PUBLIC_TOPICS` byte-identical, **0 new dependencies**, **0 files changed** in `autonomy-engine`, `personality-engine`, `action-engine`, `ws-gateway`, `perception-engine` or `agent-os`. `phase-4` untouched at `f39fa6c`; `main` untouched at `7e273e6`; **no `phase-4f` branch exists**. Last code-bearing head `a044782`; current head **`ebef196`**, this Gate Review pass's documentation-only commit, with `git diff a044782..ebef196 -- ':!docs'` **empty** and CI **37/37 green again** at it | **35,733** (comparable scope) / **41,074** (4C.2's wider scope) / **44,706** (full, incl. `apps/*/src`) — measured at head `a044782`. Growth is reported against the branch base measured with **identical scope strings** (34,469 / 39,810 / 43,114), which **reproduces Phase 4D's corrected closing values exactly**: **+1,264 / +1,264 / +1,592**. **~50,000 gate NOT crossed — 5,294 to spare on the widest scope** | **Not reported** — production scopes and the metric set only; not backfilled | **`cloc` v2.06**, `--skip-uniqueness --quiet`, **from pristine `git archive` extracts** — the tool, version *and* extraction discipline 4D's Gate Review §22.6 corrected to, so the series continues comparably. §2 needs no new entry: neither tool nor scope definitions changed. The `scc` series stays separate; the Option A/B decision in §2 **remains open** | **2,897 passing, 0 failing** (2,614 via `npx turbo run test --force`, 31/31, `Cached: 0`, 51.099 s, + 283 `tools/tests`); 384 deselected. lint 31/31 · typecheck+build 34/34 · import-linter **7 kept, 0 broken** · codegen **116 files, zero drift**. **All 10 of TDD §14.5's negative controls demonstrated**, two of them by the type system rather than only by a test; **10 mutations** each failed a named test | `nova_digital_twin_engine.domain` **100%** (357 stmts / **0** miss) against the 85% gate. `api-gateway` and `apps/web-client` are not coverage-gated (the gate is Python-only) | **FULLY GREEN — 37/37 check runs `success` at the exact head `a044782`**, zero failed, zero cancelled: PR Checks #102 ([`34954932427`](https://github.com/Tudor191/NOVA-PROJECT-BIBLE/actions/runs/34954932427)), Real-Infrastructure Checks #140 ([`34954932413`](https://github.com/Tudor191/NOVA-PROJECT-BIBLE/actions/runs/34954932413), 14/14), Build & Scan #102 ([`34954932430`](https://github.com/Tudor191/NOVA-PROJECT-BIBLE/actions/runs/34954932430), 21/21 incl. Trivy). **Playwright 18 specs — 17 passed, 1 skipped, 0 failed (55.1 s)**, the 3 AC-6 specs all green after failing 3/3 at `407457d`; the one skip is 4B's `approval-lifecycle`, CF-9's fail-closed gate working as designed | **PASSED — `digital-twin-engine` 18 passed / 143 deselected / 10.47 s and `memory-engine` 4 passed / 126 deselected / 15.56 s**, in CI at `a044782`. The `memory-engine` tier is **new in 4E** and is the point of it: that engine had **no real-Postgres coverage at all**, which is exactly how the discarded-`created_at` defect shipped unseen. Docker unreachable locally throughout, so every real-infrastructure and browser result is CI's and is labelled as such | Two new documents (the Gate Review and `phase-4e.md`), one corrected living reference (`ENGINEERING_ROADMAP.md`'s 4E row, which read **"Not started"** — corrected additively, original preserved), this row, and a dated additive note on PR #29's body. **No historical section rewritten.** TDD 4E was already current and needed no edit: re-measurement at `a044782` **confirmed** its §17 SLOC table rather than superseding it. **`README.md` still carries no Phase 4 status line** — a Phase-4-level obligation whose `definition-of-done.md` item-5 triggers 4E does not fire (Gate Review §14.3), carried forward unchanged |
| [Project Health Review (2026-08-29)](../roadmap/architecture-reviews/project-health-review-2026-08-29.md) | **HEALTHY** — the second Project Health Review, triggered by SAD 15 §10's ~30,000 Production SLOC reminder crossing at Phase 3E (Gate Review condition C-5) | 31,319 / 34,446 (re-stated, not re-measured) | 120,266 | `cloc` v1.98 | N/A (review only) | N/A | N/A | N/A | Addresses all twelve Engineering Review Milestone items; 3 Limited for want of a runtime environment, so it **does not discharge the 50,000 SLOC gate** (its §0). Four findings, none blocking Phase 3E: two Phase-1 subjects with live subscribers and no registered payload (PHR-1/PHR-2, reported not fixed), 12 scaffold `TODO` placeholders (PHR-3), and `router.py` at 1,498 lines (PHR-4, observation) |

`NR` = Not reported in the source document. A blank methodology/scope cell is never inferred from a neighboring phase.

---

## 2. SLOC methodology history — read before comparing any two numbers in the table above

**Do not treat the Production SLOC column above as one continuous series.** Assembling this master index surfaced a longer, more tangled tooling history than "scc replaced cloc" — it is preserved here in full because several of the individual gate reviews' own claims of methodology continuity do not hold up against each other:

1. **Phase 1** — `cloc --skip-uniqueness`.
2. **Phase 2A** — `cloc`, **without** `--skip-uniqueness`. Phase 2B's own gate review later states plainly that this makes Phase 2A's figure not directly comparable: "having confirmed that plain `cloc` silently deduplicates identical scaffolded files (`alembic.ini`, `script.py.mako`) across engines — Phase 2A's own SLOC figures were not re-measured with this flag."
3. **Phase 2B** — `cloc --skip-uniqueness` (corrected back).
4. **Phase 2C** — `cloc --skip-uniqueness`.
5. **Phase 2D-A** — `cloc --skip-uniqueness`.
6. **Phase 2D-B** — switches to **`scc`**, disclosed explicitly, not silently. The source text itself contains an apparent internal error worth flagging rather than fixing: it calls `--skip-uniqueness` a "Phase 2D-A-era `scc` flag," when Phase 2D-A's own gate review states it used `cloc --skip-uniqueness`, not `scc` — `--skip-uniqueness` is a `cloc` flag. Preserved verbatim in `phase-2d-b.md`, not silently corrected here.
7. **Project Health Review (2026-08)** — `scc`, consistent with Phase 2D-B, re-confirms the same 31,610 figure.
8. **Phase 2D-C** — `scc`, and its own text explicitly claims this is "consistent with every prior gate review's methodology." True relative to 2D-B and the Project Health Review; **not true relative to Phases 1 through 2D-A**, which used `cloc`.
9. **Phase 2D-C Closure — Priority 5** — switches back to **`cloc`**, silently (no disclosure comparable to 2D-B's own), and additionally narrows the *scope* from `src/` + Alembic `versions/` to `services/*/src` + `packages/*/src` only (no migrations counted at all). This directly contradicts Phase 2D-C's own "consistent with every prior gate review" claim, one report later.
10. **Phase 2D-D, Phase 3A, all three Phase 3B units, Phase 3C, Phase 3D** — **no Production SLOC or Total SLOC figure reported at all.** This is the reporting gap this whole `docs/project-health/` system exists to make visible and prevent from recurring silently.
11. **This project-health audit itself** (informational only, not phase-attributed): a fresh `cloc` measurement taken directly against the post-Phase-3C merged tree — **Production SLOC: 28,742; Total SLOC: 96,262** (`services/*/src` + `packages/*/src` + Alembic `versions/`, `cloc`, excluding blanks/comments). **This number is explicitly not inserted into the historical series above as though it continues it.** It is lower than Phase 2D-C's `scc`-measured 33,175 despite substantial code having been added since — most plausibly because `cloc` and `scc` are known to disagree on how Python docstrings are counted (comment vs. code), not because code was removed. Label if cited elsewhere: **"Current informational measurement, `cloc`, not comparable to the historical `scc` series."**

12. **Phase 3E** (2026-08-29) — **`cloc` v1.98**, disclosed, continuing item 11's tool and its `services/*/src` + `packages/*/src` + Alembic-`versions` scope so the two are directly comparable: **31,319** against item 11's 28,742, i.e. **+2,577 (+9.0%)**. Phase 3E additionally reports a **second, wider scope** — the same plus `agent-os/{kernel,registry,supervisors}/src`, `agent-os/sdk/python/src`, `agents/*/src` and `agent-os/*/alembic/versions` — giving **34,446**. The two are reported **side by side and never merged into one trend**, because Phase 3E introduced two top-level directories (`agent-os/`, `agents/`) that no earlier measurement's scope could have contained. `scc` is not installed in the environment that produced these figures, so neither number is comparable to the `scc` series in items 6–8. Label if cited elsewhere: **"`cloc`, Phase 3E, comparable to item 11 only."** This entry does **not** resolve the Option A / Option B decision below.

13. **Phase 4B** (2026-09-06, Conditional-GO closure pass, condition C-4) — **`cloc` v2.06**, installed via `npm install -g cloc` because neither tool had been present in the environments that produced Phase 4B's first three passes (all of which correctly read **"Not measured"** rather than estimating). It continues item 12's tool and item 11's scope: **32,923** on the comparable scope against Phase 3E's 31,319, i.e. **+1,604 (+5.1%)**; **36,050** on Phase 3E's wider scope (identical +1,604, as expected — the two differ only by `agent-os/` and `agents/`, which Phase 4 did not touch); and a **third, wider still** scope of **38,369** adding `apps/*/src`, because **Phase 4 introduced `apps/`, a top-level directory no earlier measurement's scope could have contained** — the same situation item 12 handled for `agent-os/` and `agents/`, handled the same way, reported side by side and never merged into one trend. Total SLOC **135,118** excluding generated TypeScript / **137,280** including it. `--skip-uniqueness` is reported beside the comparable figure (**32,979**, +56) because that flag's presence or absence has broken comparability in this project twice already (items 2–3). **The one genuine caveat: the tool *version* changed, 1.98 → 2.06.** `cloc`'s language definitions and comment handling change between releases, so an unknown part of the +1,604 may be the tool rather than the code; the figures therefore carry the version and are **not** claimed exact-to-the-line comparable with item 12, only directionally so. Label if cited elsewhere: **"`cloc` v2.06, Phase 4B, directionally comparable to items 11–12, not to the `scc` series."** This entry does **not** resolve the Option A / Option B decision below. **SLOC milestones:** the ~30,000 reminder was crossed at Phase 3E and discharged by the 2026-08-29 Project Health Review; the **~50,000 gate is not crossed** — the highest figure on any scope is 38,369, leaving ~11,631 of headroom.

**Open methodology decision, left explicitly open, not decided here:**

- **Option A** — restore `scc` as the measurement tool (matching Phase 2D-B through Phase 2D-C, the most recent tool actually used when a figure was still being reported) and install it in every environment that produces a Gate Review.
- **Option B** — formally establish `cloc` (with a fixed, documented scope and flag set) as the new baseline going forward, explicitly superseding the `scc` series rather than trying to reconcile with it.

**This decision requires the user's explicit approval and is not made by this document.**

---

## 3. Historical records index

Individual, per-phase snapshot files, each citing its own source document by exact line number wherever a value is drawn from one:

- [`phase-1.md`](phase-1.md)
- [`phase-2a.md`](phase-2a.md)
- [`phase-2b.md`](phase-2b.md)
- [`phase-2c.md`](phase-2c.md)
- [`phase-2d-a.md`](phase-2d-a.md)
- [`phase-2d-b.md`](phase-2d-b.md)
- [`phase-2d-c.md`](phase-2d-c.md)
- [`phase-2d-c-closure.md`](phase-2d-c-closure.md) (Priority 5 only — Priorities 1-4 and 6 have no dedicated Gate Review document of their own found during this research pass, and are therefore not separately recorded here)
- [`phase-2d-d.md`](phase-2d-d.md)
- [`phase-3a.md`](phase-3a.md)
- [`phase-3b.md`](phase-3b.md) (covers PR #2 — Domain Foundation, PR #7 — Decomposition Orchestration, and `phase-3b-planning-persistence` — Planning Persistence, as three clearly separated sub-units, per their own separate Gate Reviews)
- [`phase-3c.md`](phase-3c.md)
- [`phase-3d.md`](phase-3d.md)
- [`phase-3e.md`](phase-3e.md)
- [`phase-4b.md`](phase-4b.md)
- [`phase-4e.md`](phase-4e.md) (one unit, **closed and merged** 2026-09-15 as `59bbeee`; gate verdict **GO** with **no conditions attached**, CI 37/37 green at the merged head `df4606c`, AC-6 Met at four tiers. `phase-4e` preserved at `df4606c`. **CF-9, CF-10 and CF-11 stay OPEN**, and three findings are carried as explicitly non-blocking. *Previously: "complete and verified, NOT merged … PR #29 is open"*)
- [`phase-4d.md`](phase-4d.md) (one unit, **closed and merged** 2026-09-14 as `68397a2`; final gate verdict **GO**, all five conditions C-1…C-5 discharged, CI 36/36 green at the head SHA. **CF-9, CF-10 and CF-11 stay OPEN.** *Previously: "one unit, unmerged … C-4 and C-5 open"*)
- [`phase-4c.md`](phase-4c.md) (covers **4C.1** `agent-os` containerization, PR #25, and **4C.2** the Agents surface, PR #26, as two clearly separated sub-units — the Phase 3B multi-unit precedent. **4C.1 has no Gate Review document of its own**; its fields are cited to the additive implementation notes it wrote into [`00-master-scope.md`](../design/phase-4/00-master-scope.md) §5 and §10, and that gap is recorded in its field 21 and carried to Phase 4 closure)

**Closed 2026-09-15 (Phase 4E closure pass):** Phase 4E is **complete, verified,
merged and closed**. PR #29 merged into `phase-4` on explicit user authorization
as **`59bbeee3838290ab31b0627b64c6fc0b2bceb13b`** — a normal two-parent merge of
`f39fa6c` and `df4606c`, no squash, no rebase, no force push. `phase-4e` is
preserved at `df4606c`, not deleted, and is a git ancestor of `phase-4`; all ten
4E commits remain reachable. `main` is untouched at `7e273e6`, still frozen per
master scope §16 rule 2. **Phase 4 now stands at 4A–4E merged, with 4F
remaining**; **`phase-4f` has not been created and no Phase 4F work has
started.** This supersedes the *"It is **not merged**"* sentence in the note
directly below, which was correct through head `df4606c`; every other word of
that note stands.

**CF-9, CF-10 and CF-11 remain OPEN and are inherited by Phase 4's closure.**
Neither the GO nor the merge closes any of them. **All three Phase 4E findings
also remain OPEN** — `nova_testkit`'s Postgres image drift, the D-6 prefix
exposing six pre-4E operations that take a caller-supplied `user_id`, and the
stranded-outbox guard that does not yet watch the `memory` schema. `README.md`
still carries no Phase 4 status line; that is a Phase-4-level obligation, and
4E's merge no more discharges it than 4D's did.

**Added 2026-09-15 (Phase 4E):** `phase-4e.md` is added here and given its §1
timeline row. Phase 4E is **one** milestone and creates no new one — the Phase 4
milestone set remains 4A–4F. Its verdict is **GO with no conditions attached**:
all eleven of protocol §3.2's GO conditions hold, AC-6 is **Met** at unit,
integration, real-Postgres and browser tier, and CI is **37/37 green at the exact
head `a044782`**. It is **not merged** — PR #29 is open and merge authorization
is separately the user's, so unlike Phase 4D's closure this GO records
completeness, not landing.

> **Two additive corrections to the 2026-09-14 note below** (protocol §0.3.4 —
> the original wording stays): its *"`phase-4e` has not been created"* was
> correct on 2026-09-14 and is superseded — `phase-4e` was branched from
> `phase-4`'s merged head `f39fa6c` on 2026-09-15. Its *"Phase 4 now stands at
> 4A–4D merged, with 4E and 4F remaining"* still holds for the **merged** set:
> 4E is complete and unmerged, so **4F alone remains unstarted**, and no
> `phase-4f` branch exists.
>
> *(Superseded later the same day by the closure note above: 4E **is** now merged,
> as `59bbeee`, so the merged set is 4A–4E and 4F alone remains. The rest of this
> paragraph is unaffected — 4F is still unstarted and `phase-4f` still does not
> exist.)*

**Phase 4E creates no new carry-forward, and closes none.** **CF-9, CF-10 and
CF-11 all remain OPEN** — CF-10 asserted as five separate unresolved properties
by 4E's own controls, per ratified TDD 4E §19.2. Three findings are carried as
**explicitly non-blocking**: `nova_testkit`'s Postgres image drift
(pre-existing), the D-6 prefix exposing six pre-4E operations that take a
caller-supplied `user_id` (reported not fixed, pinned by two tests), and a
stranded-outbox guard that does not yet watch the `memory` schema. None affects
an acceptance criterion. `README.md`'s missing Phase 4 status line is
**unchanged** and remains a Phase-4-level closure obligation.

**Closed 2026-09-14 (Phase 4D closure pass):** Phase 4D is **complete, merged
and closed**. PR #27 merged into `phase-4` on explicit user authorization as
**`68397a200f08db497fc4f36a1b14422dc953298e`** — a normal two-parent merge of
`dd6a147` and `dff646c`, no squash, no rebase, no force-push. `phase-4d` is
preserved at `dff646c`, not deleted, and is a git ancestor of `phase-4`;
`main` is untouched at `7e273e6`. **Phase 4 now stands at 4A–4D merged, with
4E and 4F remaining**; `phase-4e` has not been created.

**CF-9, CF-10 and CF-11 remain OPEN and are inherited by Phase 4's eventual
closure.** None is closed by 4D's GO, and the carry-forward register in
[`00-master-scope.md`](../design/phase-4/00-master-scope.md) §4 and Gate Review
§13.1 is where each is dispositioned. One further correction landed with the
closure: the Phase 4D Gate Review §12's three Production SLOC figures were each
**56 low**, uniformly at both endpoints, so the reported **+1,465** growth is
correct while the absolute figures are not — the verified values are
**34,469 / 39,810 / 43,114** (`cloc` v2.06, `--skip-uniqueness --quiet`, from
pristine `git archive` extracts). Neither SLOC milestone is crossed.

**Updated 2026-09-14 (Phase 4D final Gate Review):** Phase 4D's verdict is now
**GO** (Gate Review §22). **All five conditions C-1…C-5 are DISCHARGED** and CI
is **36/36 green at head `9afddf6`**, zero failed and zero cancelled. C-5 was
fixed at its root by **PR #28** — a repository-maintenance branch correcting the
stale `tools/scaffold-engine.py` Dockerfile template that had left three images
unhardened — merged into `phase-4` as `dd6a147` and inherited by `phase-4d`;
C-4 was discharged by a ratified documentation-only reconciliation of TDD §5.3
and §4.1, creating no Event Bus subject and no RPC. **CF-9, CF-10 and CF-11 all
remain OPEN**, each disclosed and outside 4D's ratified boundary, and GO does not
close them. **PR #27 is still open and not merged**; `main` remains `7e273e6`.

**Updated 2026-09-13 (Phase 4D CI verification pass):** PR #27 was opened solely
to obtain CI evidence, with merge authorization explicitly withheld. **C-1, C-2
and C-3 are discharged**; **C-4 remains open** and **C-5 is new and open**
(`build-and-scan (ws-gateway)`, a pre-existing CVE failure this branch is not
permitted to fix). **CF-9, CF-10 and CF-11 all remain OPEN**, the latter two
re-verified at source rather than carried forward on trust. Still **not merged**.
*(C-4 and C-5 were both discharged the following day — see the 2026-09-14 note
above.)*

**Added 2026-09-13 (Phase 4D):** `phase-4d.md` is added here and given its §1
timeline row. Phase 4D is **one** milestone and creates no new one — the Phase 4
milestone set remains 4A–4F. Its verdict is **CONDITIONAL-GO with four open
conditions, none of which is an approved deferral**, so unlike Phase 4B's and
Phase 4C's it must **not** be read as equivalent to GO: protocol §3.2 condition 6
is unmet outright, because no CI run exists. Three new carry-forwards are
recorded there — **CF-10** (no read surface for 2D-D's `TrustMetric`), **CF-11**
(no producer of suggestions) and **CF-12** (two test tiers unexecuted) — and
**CF-9 stays OPEN** by ratified decision D-4D-2.

> **Superseded in part by the 2026-09-13 CI verification note above** (protocol
> §0.3.4 — corrections are additive, so the original wording stays). Three of its
> claims were falsified by evidence obtained later the same day, on PR #27 at head
> `f5f263f`: (a) *"no CI run exists"* — 36 checks ran, so condition 6 is now
> **assessable and partly met**, failing only on `build-and-scan (ws-gateway)`,
> which is C-5; (b) **CF-12** (two test tiers unexecuted) is **CLOSED** — the
> 16-test `real-infra (autonomy-engine)` tier and both AC-5 Playwright specs
> executed and passed in CI, so two carry-forwards remain new, not three; (c) the
> count of open conditions is **two** (C-4, C-5), not four, C-1/C-2/C-3 having
> been discharged. **CF-9, CF-10 and CF-11 remain OPEN as written**, and the
> verdict is still **CONDITIONAL-GO**, still not equivalent to GO, still unmerged.

**Added 2026-09-12 (Phase 4C closure pass):** `phase-4c.md` is added here and
given its §1 timeline row. Phase 4C is exactly **4C.1 and 4C.2**; there is no
4C.3, 4C.4, 4C.5 or 4C.6, and none is created by that record. Its **C-3 remains
open by approved deferral** — AC-4 clauses 2 and 3, Deferred by explicit user
approval of 2026-09-07, with provider configuration the only remaining trigger.

**Added 2026-09-06 (Phase 4B closure pass):** `phase-3e.md` was missing from this
index — it has existed since 2026-08-29 and has always had a §1 timeline row, but
was never listed here. `phase-4b.md` is added at the same time. **There is no
`phase-4a.md`**: Phase 4A merged into `phase-4` (PR #23) without either a Gate
Review or a Project Health record, so per [`README.md`](README.md) it is **not
fully closed**. That gap is recorded in [`phase-4b.md`](phase-4b.md) field 21 and
analysed in the [Phase 4B Gate Review](../roadmap/architecture-reviews/phase-4b-observability-panels-gate-review.md)
§13.1, which finds it does not gate Phase 4B; it is carried forward so `phase-4`'s
eventual closure inherits it. Phase 4A itself is left untouched.

The one-time [Project Health Review (August 2026)](../roadmap/architecture-reviews/project-health-review-2026-08.md) is referenced throughout this index but intentionally kept in its existing canonical location under `docs/roadmap/architecture-reviews/` rather than duplicated here — it is a comprehensive, cross-phase architecture audit, not a per-phase snapshot, and per SAD 15 §10 it is triggered by the 30,000/50,000 SLOC milestones rather than by phase boundaries.

---

## 4. Current Project Health Monitoring classification

**`ACTIVE BUT NOT REPORTED`** — carried forward unchanged from the audit that established this classification. The Project Metrics / Production SLOC requirement under SAD 15 §10 remains standing, permanent policy (never rescinded), and the tooling to compute it still works. It has not been included in the last several phase reports (§1 above shows the exact pattern: full compliance through Phase 2D-B, degraded to a bare delta for 2D-C/3A, absent entirely from 2D-D, all three Phase 3B units, Phase 3C, and Phase 3D). This document does not claim monitoring was removed, and does not claim the historical `scc`-era and `cloc`-era measurements are currently comparable.

See `README.md` for how this classification is meant to be kept current going forward.
