# Phase 2D-B — Project Health Snapshot

Source: `docs/roadmap/architecture-reviews/phase-2d-b-gate-review.md` (extracted via full-text research pass; every value below is cited to a specific line in that document). This is also the phase at which the **30,000 SLOC Project Health Review reminder** (SAD 15 §10) was first crossed, triggering `docs/roadmap/architecture-reviews/project-health-review-2026-08.md` — see the master index's SLOC History section and that document directly for the full, independent, cross-phase review it triggered.

| # | Field | Value |
|---|---|---|
| 1 | Phase identity | Phase 2D-B |
| 2 | Phase/Sub-Phase name | Identity & Presence. Covers `perception-engine` (new), the `ai-model-orchestration-engine` biometric/wake extension, and the World Model Engine `present_identities` extension. |
| 3 | Report date | Not stated explicitly in the document's own text (no "Date:" field, no calendar-date string found anywhere in the file). File's last commit date (fallback, not a phase-stated date): **2026-08-07 17:43:33 +0000**. |
| 4 | Branch | Not reported |
| 5 | PR number | Not reported |
| 6 | Commit / merge commit | Not reported |
| 7 | Phase status | "**Go.** Every check available in this environment passes: 951 tests, zero ruff/mypy/import-linter issues, zero dependency cycles, zero cross-engine coupling, mechanical verification of the addressee-signal and wildcard-subject boundaries, and two real bugs plus one stale generated artifact found and fixed by this phase's own verification discipline before being reported as complete." (`:344-348`) |
| 8 | Production SLOC | **31,610** — application `src/` (30,472) + Alembic `versions/` only (1,138, 9 files, one per engine) (`:376-380`). Prior phase (2D-A) baseline in the same table: 20,969 (`:390`). This is the value that first crossed the 30,000 SLOC Project Health Review reminder threshold. |
| 9 | Total SLOC | **77,286** (all tracked languages, all purposes), up from 61,387 (`:384`) |
| 10 | SLOC methodology/tool | **`scc`** — a change from Phase 2D-A's `cloc`. Disclosed explicitly, not silently, quoted verbatim from the source: "`scc` — this session's own tool choice; `--skip-uniqueness` was a Phase 2D-A-era `scc` flag no longer present in the `scc 3.7.0` binary available here, so plain `scc` was used instead, noted honestly rather than silently assumed equivalent" (`:358-363`). **This is the first documented tool change in the SLOC series.** **Source inconsistency preserved, not silently corrected:** the source text calls `--skip-uniqueness` a "Phase 2D-A-era `scc` flag," but Phase 2D-A's own gate review (`phase-2d-a.md` in this same directory) states it used `cloc --skip-uniqueness`, not `scc` — `--skip-uniqueness` is in fact a `cloc` flag (confirmed independently against `METRICS_TEMPLATE.md`'s own tooling note). This document reports the source's exact wording rather than silently fixing the apparent error, per the strict no-reconciliation rule. |
| 11 | SLOC scope | Application `src/` + Alembic `versions/` only (9 files, one per engine); dev tooling, tests, generated TS, docs reported separately (`:376-380`) |
| 12 | Test status | "**951 tests pass** across all 17 first-party packages (up from Phase 2D-A's 804)." (`:14`) |
| 13 | Test count | 951 (up from 804) (`:14, 435`) |
| 14 | Coverage | Aggregate over 10 production services: **78.7%** (10,952 stmts, 2,330 missed) — "essentially flat" vs. Phase 2D-A's 79.0% (`:441`) |
| 15 | CI status | Ruff: PASS, 0 issues. MyPy: PASS, 413 files across 17 packages, per-package (matching CI exactly). import-linter: PASS, 4/4 contracts, 395 files / 1,783 deps (`:442-444`) |
| 16 | Real-infrastructure status | "No real-Postgres verification was performed for `perception-engine`'s repository layer this phase — the same open item carried forward from Phase 2D-A for `personality-engine`/`communication-engine`, now a three-engine backlog. No Docker daemon has been reachable in this session's environment." (`:60-64`) |
| 17 | Documentation health | Documentation growth: 25,891 → **25,947** lines (+56). Architecture documents: 100 in `docs/` + 17 READMEs = **117 total** (`:464, 423`) |
| 18 | Architecture status | "The architecture is sound by every check available in this environment. This phase's most significant finding is the same one Phase 2D-A's review named: a verification-depth gap (real-Postgres) that keeps growing rather than closing, now covering three of ten engines. Reported here plainly, not smoothed over." (`:66-69`) |
| 19 | Security status | Consent (`require_active_consent`) gates every capture-adjacent operation; templates encrypted before the repository layer ever sees them (Fernet boundary, dedicated tests); revocation is a verified hard delete; revocation synchronously stops the matching sensor; no identity signal can authorize anything by itself (ADR-032, structural — no gating logic to audit); `template_encryption_key` empty by default in dev mode, documented, enrollment fails explicitly rather than silently encrypting (`:128-149`) |
| 20 | Unverified infrastructure | No Docker-capable environment (blocks real-Postgres verification for three engines; blocks running the full compose stack end-to-end); no live audio/camera capture client exists anywhere in the project — every new AIMO connector and perception-engine sensor is exercised only against fakes, "none has ever received a real audio window or camera frame" (`:101-114`) |
| 21 | Open blockers | None named as blocking — an outright "Go," no conditional decision required (contrast with 2D-A's Recommendation-10 flag). Separately: the 30,000 SLOC threshold crossing is reported as "a reminder, not an automatic pause: the user decides." (`:344, 470-482`) |
| 22 | Branch hygiene | Not reported |
| 23 | Notes / important findings | (a) Two real implementation bugs found and fixed by this phase's own test suite before the review was written: mathematically-unreachable presence-detection thresholds in both sensors, and a shutdown crash on an already-stopped sensor — "Neither was caught by ruff or mypy — both required actually exercising the code." (`:48-54`) (b) A stale generated artifact found and fixed: `ContextChangedPayload.ts` had drifted from its Python schema source (`:55-59`). (c) `ai-model-orchestration-engine/domain/router.py` reached **1,384 SLOC**, "the single largest file in the codebase," after two consecutive modality extensions (`:86-89`). (d) A wider mypy check (whole tree, not just `src/`) caught a real pre-existing gap in `test_router.py`'s fake connectors dating to Phase 2D-A, fixed this session (`:22-27`). (e) Complexity outliers noted as pre-existing, not new: `PipelineOrchestrator.run` (D, 27) and `session_websocket` (D, 22) — "No `perception-engine` function appears in the top 10 most complex blocks in the codebase." (`:500`) |
