# Phase 4D — Project Health Snapshot

Source: the
[Phase 4D Gate Review](../roadmap/architecture-reviews/phase-4d-autonomy-engine-gate-review.md),
written against head `de6dbc979ada8b46b07de9bea195808f0a2a934d` on `phase-4d`.

**Phase 4D is one unit, unmerged. CI evidence was obtained on 2026-09-13** by
opening PR #27 solely for that purpose; merge authorization was not granted.

> **Updated 2026-09-13 (CI verification pass).** This record was first written
> against head `5a2bf77`, when **no CI run existed at any SHA**. Fields 4, 6, 13,
> 15, 16, 20 and 21 are updated below with the evidence that pass obtained, and
> each supersession is marked rather than silently rewritten. The original
> wording is preserved inline where a figure changed. Nothing is inferred,
> simulated or backfilled.

---

| # | Field | Value |
|---|---|---|
| 1 | Phase identity | Phase 4D — the Autonomy Engine |
| 2 | Phase/Sub-Phase name | The Autonomy Engine (Bible Part 14) and the `autonomy/` panel, in four implementation slices: the domain layer (`eb48d0f`) · persistence, the `/v1/autonomy/*` REST surface and the boundary controls (`5eae4c2`) · the `real_infra` tier, both CI matrices, the `api-gateway` route and the compose service (`152d347`) · the panel, the AC-5 Playwright spec and its producer driver (`de6dbc9`). Master scope §5 and §6; TDD [`04-tdd-4d-autonomy-engine.md`](../design/phase-4/04-tdd-4d-autonomy-engine.md) |
| 3 | Report date | 2026-09-13 |
| 4 | Branch | `phase-4d`, **last code-bearing head `f5f263fc10e52c93bbf18e2dd1f37e1dcd4038f5`**, with every later commit documentation-only — the invariant is `git diff f5f263f..HEAD -- ':!docs'` being empty (Gate Review §21.10). *(This field originally read `de6dbc9`; `32b6dcd` fixed the three failures the first CI run found, `f5f263f` remediated a pre-existing CVE blocker, and `41ab3fc` onward record this pass's evidence.)* Pushed to `origin`. Branched from `phase-4`'s merged head `eedb8ade956862cc9230a2ea5fa3c33c36391aed` per master scope §16 rule 5. **Linear, zero merge commits** — `f5ca915` the TDD correction recording decisions D-4D-1 and D-4D-2, the four implementation slices (`eb48d0f`, `5eae4c2`, `152d347`, `de6dbc9`), `5a2bf77` the Gate Review, then the CI-pass commits. *(The count originally read "Six commits", true at `5a2bf77`.)* Working tree clean, nothing unpushed |
| 5 | PR number | **#27**, `phase-4d` → `phase-4`, opened 2026-09-13 **solely to obtain CI evidence**; merge authorization explicitly not granted, and it is **not merged**. *(This field originally read "None. No pull request was opened" — correct for head `5a2bf77`.)* Opening it discharged C-1, C-2 and C-3 |
| 6 | Commit / merge commit | **Last code-bearing head `f5f263fc10e52c93bbf18e2dd1f37e1dcd4038f5`**; chain `eb48d0f` · `5eae4c2` · `152d347` · `de6dbc9` · `5a2bf77` · `32b6dcd` · `f5f263f`, then documentation-only commits (`41ab3fc` onward) recording this pass's evidence. **No merge commit — nothing is merged.** `phase-4` and `main` are untouched; `phase-4` remains `eedb8ad` and `main` remains `7e273e6` |
| 7 | Phase status | **Implementation complete, CI-verified, and NOT merged; gate verdict CONDITIONAL-GO — unchanged.** **C-1, C-2 and C-3 are DISCHARGED** by the 2026-09-13 verification pass (Gate Review §21): CI ran and is green for every check exercising 4D, the 16-test `real_infra` tier passed twice, and both AC-5 Playwright specs passed in a real browser. **C-4 remains OPEN** (TDD §5.3 / §4.1 reconciliation, offered for ratification) and **a new C-5 is OPEN** — `build-and-scan (ws-gateway)` fails on 12 pre-existing Debian CVEs that this diff does not touch and that this branch is not permitted to fix. **Neither open condition is an approved deferral**, so the verdict must still not be read as GO; protocol §2.1's "partially met is not met" is unaltered. *(This field originally recorded four open conditions with C-1 as the root of C-2 and C-3 — correct for head `5a2bf77`.)* |
| 8 | Production SLOC | **34,413** on the scope comparable to Phase 4B's and 4C.2's (`services/*/src` + `packages/*/src` + `services/*/alembic/versions`); **39,754** on 4C.2's wider scope (adding `agent-os/*/src`, `agent-os/*/alembic/versions`, `agents/*`); **43,058** on the full scope also including `apps/*/src`. Measured at head `de6dbc9`. **Growth is reported against the branch base measured with identical scope strings**, not against the 4C.2 figures as published: base `f5ca915` gives **32,948 / 38,289 / 40,974**, and the middle figure **reproduces the 4C.2 health record exactly**, which is what makes the series continuous. Deltas: **+1,465 / +1,465 / +2,084**. The comparable and wider scopes move identically because 4D's engine code lands entirely in `services/`; the wider gap on the third scope is the panel and entity module under `apps/` |
| 9 | Total SLOC | **Not reported.** This review reports production scopes and the metric set, not a whole-repository total. Not backfilled |
| 10 | SLOC methodology/tool | **`cloc` v2.06** — the same tool *and* version Phase 4B's and 4C.2's closure passes used, so this figure continues that series comparably rather than only directionally. The **`scc` series (2D-B → 2D-C) remains separate and non-comparable**, and the Option A / Option B methodology decision in [`project-health-master.md`](project-health-master.md) §2 **remains open and is not decided here** |
| 11 | SLOC scope | Three scopes reported side by side, continuing 4C.2's shape. **§2 needs no new methodology entry** — neither the tool nor the scope definitions changed. Both revisions were measured with identical scope strings, so the deltas are comparisons rather than estimates |
| 12 | Test status | **Local and CI.** Local: `npx turbo run test --force` **31/31, `Cached: 0`**; lint **31/31**; typecheck **5/5**; `uv run lint-imports` **7 kept, 0 broken**; codegen **116 files, zero drift** (4D adds no wire contract); **2,715 passing, 0 failing** (2,491 turbo + 224 `tools/tests`). CI at `f5f263f`: the `checks` job — which runs that same battery — **success**, alongside `real-infra (autonomy-engine)`, `Playwright golden path` and `dependency-audit`. All twelve of TDD §16's negative controls remain demonstrated by property removal; none was weakened by the CI-fix pass. *(This field originally read "All local; no CI" — correct for head `5a2bf77`.)* |
| 13 | Test count | `autonomy-engine` **238 collected** — 222 default tier (166 unit, 38 integration, 18 contract) + **16 `real_infra`, all 16 passing in CI**. `api-gateway` 88. `apps/web-client` 199, of which **24** are the new panel suite. `tools/tests` **246** (up from 224: the new `test_dockerfile_workspace_deps.py` guard adds 22). **Playwright: 2 AC-5 specs written and both executed and passed in CI** — 972 ms and 377 ms, inside `1 skipped, 14 passed (51.7 s)`. *(This field originally read "Playwright: 2 specs written, 0 executed" — correct for head `5a2bf77`.)* |
| 14 | Coverage | `nova_autonomy_engine.domain` **99%** (350 statements, 3 missed) against the 85% `fail_under` gate. `api-gateway` and `apps/web-client` are not coverage-gated — the gate is `[tool.coverage.report]` in `pyproject.toml` and applies to Python domain packages only |
| 15 | CI status | **RAN, and green for every check that exercises 4D.** 36 checks against head `f5f263f`: **28 success, 1 failure, 7 cancelled**. The single failure is `build-and-scan (ws-gateway)` — pre-existing, 0 lines of this diff, Gate Review condition **C-5** — and its fail-fast cancelled the seven. **No check exercising Phase 4D failed:** `checks` **success**, `real-infra (autonomy-engine)` **success**, `Playwright golden path` **success**, `dependency-audit` **success**, 11 other `real-infra` jobs **success**. `build-and-scan (autonomy-engine)` recorded **Build success + Scan success** at `32b6dcd`, and the image is byte-identical at `f5f263f` (`git diff 32b6dcd..f5f263f -- services/autonomy-engine/` is empty), where the job was cancelled mid-build by the ws-gateway fail-fast. **Confirmed by a second full run at `41ab3fc`** (documentation-only above `f5f263f`): PR Checks **success**, Real-Infrastructure Checks **success** (14/14), Build & Scan **failure on `ws-gateway` alone** — which this time *ran and failed* rather than being cancelled, turning C-5 from predicted into observed, and left `build-and-scan (autonomy-engine)` **success** as a completed job rather than an inference (Gate Review §21.10). *(This field originally read "NONE — no workflow run exists at any SHA on this branch", which was correct for head `5a2bf77`.)* |
| 16 | Real-infrastructure status | **PASSED. 16 passed, 222 deselected, 8.64 s**, green on two consecutive heads (`32b6dcd`, `f5f263f`). Every property TDD §16 names as only-real-Postgres-provable is now proven rather than asserted: the transaction coupling and its foreign key, append-only against the real `ON DELETE RESTRICT`, keyset pagination across a timestamp tie, and doc 07's column definition. **The tier earned its place on its first run**, which found two failures — both in the *test code* of this milestone, not the implementation: `pytest.raises` is a synchronous context manager and was combined with an async one in a single `async with`. The other 14 passed first time; the assertions were not weakened, and nesting the `with` is what makes them execute. Docker remained unreachable locally throughout, so every real-infrastructure result is CI's and is labelled as such. *(This field originally read "NOT EXECUTED", correct for head `5a2bf77`.)* |
| 17 | Documentation health | Two new documents (this record and the Gate Review) plus two reconciled living references (`project-health-master.md`, `ENGINEERING_ROADMAP.md`). **No historical section rewritten.** The Gate Review records, rather than silently fixes, two TDD clauses the repository contradicts: §5.3's Event Bus trust read and §4.1's diagram edge, both residue from before D-4D-1 that its correction pass missed. Reconciling them is offered for ratification as condition **C-4**, in the same shape D-4D-1 and D-4D-2 were ratified |
| 18 | Architecture status | New engine `services/autonomy-engine`, scaffolded by `tools/scaffold-engine.py` — so `nova_autonomy_engine` entered `pyproject.toml`'s `root_packages` and all four import-linter contracts without hand-editing. New `autonomy` Postgres schema, **5 tables, 0 existing tables altered**: doc 07's canonical `decision_log` implemented as written, plus `autonomy_level`, `policy`, `permission_grant` and `suggestion`. One `api-gateway` `UpstreamRoute` (`/v1/autonomy`, forwarded 1:1 per D-6), one compose service (port **8019** — 8014 was already the api-gateway's, caught before it would have collided at `docker compose up`), one `run-migrations.sh` entry (the fourteenth engine), and matrix entries in both `build-and-scan.yml` and `real-infra-checks.yml`. The stale component counts in the compose README, the migrator Dockerfile and `run-migrations.sh` were off by one and are corrected in place as living references |
| 19 | Security status | **Nothing weakened; one control strengthened.** `PUBLIC_TOPICS` **byte-identical** — `git diff` restricted to `services/ws-gateway/` is **0 lines**. Both engine allow-lists are empty frozensets, and the assertion goes further than that: `test_control_8_this_engine_never_calls_the_bus_at_all` proves no `publish`/`subscribe`/`request`/`serve`/`open_stream` call exists anywhere in the package, so there is no site at which a subject could be introduced. **No `autonomy.*` subject is registered anywhere in the repository**, and `action-engine`'s `test_fork_e2_namespace_boundary_never_uses_autonomy_prefix` still passes **unmodified**. `action-engine` and `nova-contracts` are untouched — `git diff --name-only` matching either returns **0**. ADR-025's single trusted user preserved: `Settings`' only identity field is `primary_user_id`, and no role, RBAC, tenant or scope concept exists, asserted over code with docstrings stripped. **0 new dependencies** |
| 20 | Unverified infrastructure | **Substantially reduced by the CI pass.** (a) The `real_infra` tier **executed** — field 16. (b) The AC-5 Playwright spec **executed in a real browser against the real stack**: `autonomy-suggestion.spec.ts` **2 of 2 passed** (972 ms, 377 ms) within `1 skipped, 14 passed (51.7 s)`; the one skip is 4B's `approval-lifecycle` spec, which skips when the Critical action is denied at `action-engine`'s identity-confidence gate — CF-9's fail-closed default working as designed, pre-existing and not 4D's. (c) The compose service, migration entry and both CI matrix entries **started for real**: the `nova-local-autonomy-engine-1` container ran in the e2e stack, and the panel shipped as its own lazy chunk (`AutonomyPanel-BzN1dAed.js`, 11.44 kB). **Still unverified:** nothing specific to 4D. Prometheus still scrapes only `nova-core`, carried forward unchanged. *(This field originally listed three items as unverified, correct for head `5a2bf77`.)* |
| 21 | Open blockers | **Two open Gate Review conditions, neither an approved deferral.** **C-4** — TDD §5.3 and the §4.1 diagram still describe an Event Bus trust read that cannot exist; a documentation decision offered for ratification, on which no implementation depends. **C-5 (new)** — `build-and-scan (ws-gateway)` fails Trivy on **3 CRITICAL and 9 HIGH** Debian 13.6 package CVEs. `git diff origin/phase-4..HEAD -- services/ws-gateway/` is **0 lines**: pre-existing, not this milestone's, and deliberately not fixed because modifying that component is outside this branch's ratified scope. The one-line remedy is known, is the repository's own convention in 17 of 20 images, and was proven effective in this very run — it needs an explicit decision. **C-1, C-2 and C-3 are DISCHARGED.** Three carry-forwards remain OPEN and were **re-verified at source** by the CI pass (Gate Review §21.7): **CF-10** no read surface exists for 2D-D's `TrustMetric` — `digital-twin-engine` serves exactly one RPC, **0 of 118 registered subjects** contain "trust", no HTTP trust route exists anywhere, and `BoundEventBus.request()` independently gates on the allow-list D-4D-1 requires empty; fail-closed behaviour observed, not merely described (`score` is `None` never `0.0`, and `satisfies_threshold(None, t)` is `False` for every `t` including `0.0`). **CF-11** no production producer of suggestions — **0** call sites of `insert_suggestion`, **0** of `decide()`, no handler/worker/scheduler, **0 of 11** published operations create one, with test fixtures and the Playwright driver excluded from the count as required; the *decision surface* is separately production-reachable (`api/autonomy.py:406`, `:445`) and the missing piece is strictly the upstream trigger. **CF-9 remains OPEN** by ratified decision D-4D-2 — and the CI run incidentally re-demonstrated its fail-closed default, since 4B's approval spec skipped itself when the Critical action was denied at the identity-confidence gate. **CF-12 is CLOSED** — the two unexecuted test tiers it recorded both ran in CI (Gate Review §21.9), so two carry-forwards survive this milestone, not three. *(This field originally listed four open conditions — correct for head `5a2bf77`.)* |
| 22 | Branch hygiene | `phase-4d` preserved at `de6dbc9`, pushed, **not merged**. Linear history, zero merge commits, no force push, no rebase, no squash. `phase-4` untouched; `main` untouched at `7e273e6` |
| 23 | Notes / important findings | **Two TDD clauses were found to presuppose surfaces that do not exist, and both were named rather than built around** — structurally the same error decision D-4D-2 had already identified and withdrawn for CF-9. Neither blocks AC-5. Neither was resolved by inventing the missing surface, adding an endpoint, or faking a number: the trust input reports `UNAVAILABLE` with its reason, which is exactly TDD §13's fail-closed row, and the suggestion inbox renders a healthy empty state naming its own cause. **All twelve of TDD §16's negative controls were verified by removing the property and observing the suite fail**, not by being declared — §7 of the Gate Review names the mutation and the first failing test for each. One precision failure is recorded there too, because it is the failure mode controls are prone to: the first drafts of three source-scanning controls used substring search and **fired on this engine's own docstrings** — the prose explaining why `IdentityConfidencePolicy` stays in `action-engine` names it — and were rewritten to parse the AST and inspect code only. A control that fires on its own rationale is a control that gets loosened rather than obeyed |
