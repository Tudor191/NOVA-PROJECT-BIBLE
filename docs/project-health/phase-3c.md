# Phase 3C — Project Health Snapshot

Source: `docs/roadmap/architecture-reviews/phase-3c-capability-engine-gate-review.md`, cross-checked against this session's own direct, live verification of the merged state (PR #8 merge and post-merge audits, same conversation this record was authored in).

| # | Field | Value |
|---|---|---|
| 1 | Phase identity | Phase 3C |
| 2 | Phase/Sub-Phase name | `capability-engine` (registry, install pipeline, sandboxing) |
| 3 | Report date | Not reported as a calendar date in the gate-review document's own text. Merge event occurred `2026-08-17T08:09:20Z` (PR #8's `merged_at`, confirmed live via the GitHub API during post-merge verification). |
| 4 | Branch | `phase-3c-capability-engine`, based directly on `phase-3b-planning-domain` |
| 5 | PR number | PR #8 |
| 6 | Commit / merge commit | PR #8 head `d41824805f5590fe40104ff2f45d19023a1f0f76`; merge commit `fbb5b15c6214210391078d0ccd1f6ee3c9558e7f` (merge method: standard merge commit, confirmed via `mcp__github__pull_request_read`) |
| 7 | Phase status | "Status: implementation complete, locally verified, real GitHub Actions CI green. Covers the whole of Phase 3C's scope as authorized — a single PR, not split across multiple units." Merged into `phase-3b-planning-domain`, confirmed still fully intact and passing on the merged branch as of this record. |
| 8 | Production SLOC | Not reported in this gate review as an absolute figure for this phase specifically. (A repository-wide, informational-only `cloc` measurement was taken during the later project-health audit — see the master index's SLOC History section; it is explicitly **not** a Phase-3C-attributed delta and must not be read as one.) |
| 9 | Total SLOC | Not reported in this gate review |
| 10 | SLOC methodology/tool | Not reported (no SLOC section in this gate review) |
| 11 | SLOC scope | Not reported |
| 12 | Test status | "`capability-engine` test suite (`-m "not real_infra"`): 59/59 passed" |
| 13 | Test count | 59 passed, 6 deselected (`@pytest.mark.real_infra`) |
| 14 | Coverage | 97% domain coverage (`domain/pipeline.py` 96%, every other domain module 100%), vs. an 85% gate, measured with `--cov-report=term-missing` (branch coverage per root `pyproject.toml`) |
| 15 | CI status | "GitHub Actions CI (all 3 workflows: PR Checks, Build & Scan, Real-Infrastructure Checks): 20/20 check runs green on PR #8 at commit `6aee23d`" (post-correction state). Independently re-confirmed live during post-merge audit in this same session: the same 20 check runs re-queried fresh, still `success` (build-and-scan run `32005257595`, pr-checks run `32005257605`, real-infra run `32005257612`). |
| 16 | Real-infrastructure status | Real-Postgres persistence tests (6 tests: insert/find/list/delete/duplicate-rejection/installation-event) — real-infrastructure-verified via CI after `real-infra-checks.yml`'s matrix was corrected to include `capability-engine` (a genuine gap found and fixed mid-review: the first CI run's "green" result had not actually covered this engine at all). Alembic migration executed for real against a throwaway `postgres:16-alpine` testcontainer (run `32005017275`, job `95312607118`, green). Docker build for `services/capability-engine/Dockerfile` verified for real by CI (run `32005017282`, job `95312607250`, green) after fixing a missing `nova-service-kit` copy step. |
| 17 | Documentation health | **Known gap, confirmed by this same project-health audit process**: the canonical `phase-3b-planning-domain` branch (where this Phase 3C work lives) still carries the *pre-decision* version of `docs/design/phase-3/06-tdd-3c-capability-engine.md` (348 lines, forks framed as open proposals) rather than the *resolved* version (741 lines, forks marked "RESOLVED and approved") that exists only on the unmerged `phase-3c-research` branch. The implementation itself correctly follows the resolved decisions; the design document in the canonical lineage does not yet reflect that. Not yet synchronized as of this record. |
| 18 | Architecture status | Fork 3C-1/3D-1 = Option A (capability-engine owns/executes all adapters); Fork 3C-2 = Option C (not implemented — `AgentContext.granted_capabilities` is Phase 3E, out of scope); Fork 3C-3 = Option B (no rollback primitive; `action-engine` owns rollback later); Fork 3C-4 = Option B (natural-key `(name, version)` idempotency, DB-enforced). import-linter 6/6 contracts kept, `nova_capability_engine` correctly registered and independent. |
| 19 | Security status | Not reported as a dedicated security review section in this gate review. Sandboxing is OS-level scoping (filesystem/terminal/outbound-host allow-lists) with an explicitly disclosed limitation: a `terminal`/`git` capability's own spawned subprocess is not prevented from making its own outbound network calls, bypassing the `http` adapter's host allow-list. |
| 20 | Unverified infrastructure | Docker build and real-Postgres tests were "genuinely unverified locally" (no reachable Docker daemon in the implementation environment) — resolved via real GitHub Actions evidence instead, not left open. Nothing remains unverified as of the merge per the gate review's own tiered classification. |
| 21 | Open blockers | None blocking the merge itself. The documentation-sync gap (field 17) is an explicitly tracked, not-yet-resolved open item, addressed separately from this snapshot's own scope. |
| 22 | Branch hygiene | `phase-3c-capability-engine` fully merged, 0 unique commits vs. `phase-3b-planning-domain` (confirmed via `git merge-base --is-ancestor`), safe to delete manually (deletion blocked by a 403 permission limitation in this session's environment, not yet performed). |
| 23 | Notes / important findings | This gate review self-corrects a misleading-green CI result mid-review (field 16) rather than accepting the first "green" badge at face value — the same rigor later applied to this project-health audit's own SLOC-methodology findings. |
