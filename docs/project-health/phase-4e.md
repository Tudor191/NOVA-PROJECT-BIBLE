# Phase 4E — Project Health Snapshot

Source: the
[Phase 4E Gate Review](../roadmap/architecture-reviews/phase-4e-digital-twin-gate-review.md),
written against head `a044782360591e1d064e6dc053ce6926fc32c3e2` on `phase-4e`.

**Phase 4E is one unit, and it is CLOSED.** Final gate verdict **GO**
(2026-09-15, Gate Review §15), merged into `phase-4` as
**`59bbeee3838290ab31b0627b64c6fc0b2bceb13b`**.
*(This line read "Phase 4E is one unit. Final gate verdict GO … **Not merged** —
PR #29 is open and merge authorization is separately the user's" until closure —
correct for every head through `df4606c`.)*

> **Closed 2026-09-15 (closure pass).** Fields 4, 5, 6, 7, 17 and 22 are brought
> to the merged state; every earlier value is preserved inline and marked, per
> protocol §0.3.4. **CF-9, CF-10 and CF-11 remain OPEN**, as do all three Phase
> 4E findings — GO does not close them, the merge does not close them, and this
> record must not be read as closing them. **No Phase 4F work has started and no
> `phase-4f` branch exists.** Nothing is inferred or backfilled: every figure
> here traces to a command run against a named commit.

> Every figure in this record was **re-measured during the Gate Review** against
> `a044782`, not copied from the implementation report: CI from the GitHub
> Actions job logs at that exact SHA, SLOC from pristine `git archive` extracts,
> test counts from a fresh `turbo run test --force`. Where a re-measurement
> disagreed with the pre-Gate record, this record carries the re-measured value
> and marks it (field 16). Nothing is inferred, simulated or backfilled.
>
> **GO records that Phase 4E's own scope is complete; it closes no
> carry-forward. CF-9, CF-10 and CF-11 remain OPEN**, and this record must not
> be read as closing them.

---

| # | Field | Value |
|---|---|---|
| 1 | Phase identity | Phase 4E — the `digital-twin-engine` extension |
| 2 | Phase/Sub-Phase name | Bible Part 16's nine remaining Digital Twin domains and the `digital-twin/` panel, in eight implementation-and-documentation commits plus this Gate Review pass's `ebef196`: the AC-6 temporal-gap fix (`d39b5b8`) · the nine domains (`ae82fe0`) · the `api-gateway` prefix (`04eb77c`) · the panel and server-side identity (`87c1b32`) · the AC-6 driver, Playwright spec and CI wiring (`407457d`) · three documentation commits (`0f44cdd`, `73ea3f2`) and the deployment-topology fix (`a044782`). Master scope §1.1 AC-6; TDD [`05-tdd-4e-digital-twin-extension.md`](../design/phase-4/05-tdd-4e-digital-twin-extension.md) |
| 3 | Report date | 2026-09-15 (implementation, CI verification, Gate Review **and closure** — all the same day) |
| 4 | Branch | `phase-4e`, **final head `df4606c12628d3097645624103ead4566f4783af`**, preserved and not deleted; **last code-bearing head `a044782360591e1d064e6dc053ce6926fc32c3e2`**, with the invariant `git diff a044782..df4606c -- ':!docs'` empty (Gate Review §18). *(This field previously named `ebef196` as the current head — correct until the second documentation commit `df4606c`.)* Pushed to `origin`. Branched from `phase-4`'s merged head `f39fa6cd0b02bd4e55a42f17b0d748f636f812d4` per master scope §16 rule 5. **Nine commits, linear, zero merge commits** — eight implementation-and-documentation commits plus this pass's `ebef196`. No force push, no rebase, no squash, no history rewrite at any point. Working tree clean, nothing unpushed |
| 5 | PR number | **#29**, `phase-4e` → `phase-4`. **MERGED 2026-09-15T16:32:29Z** on explicit user authorization, as merge commit **`59bbeee3838290ab31b0627b64c6fc0b2bceb13b`** — a normal two-parent merge (no squash, no rebase), parents `f39fa6c` (previous `phase-4`) and `df4606c` (PR head). *(This field read "**OPEN and NOT MERGED**; `mergeable_state: clean`" until the 2026-09-15 authorization.)* Opened 2026-09-15 to obtain CI evidence ahead of this Gate Review, with merge authorization explicitly withheld at that time. Its body was written at `407457d` and carries a dated additive note recording the final head (Gate Review §14.4) |
| 6 | Commit / merge commit | Chain `d39b5b8` · `ae82fe0` · `04eb77c` · `87c1b32` · `407457d` · `0f44cdd` · `73ea3f2` · `a044782` · `ebef196` (this Gate Review pass, documentation only). **Merge commit `59bbeee3838290ab31b0627b64c6fc0b2bceb13b`**, two parents, resulting `phase-4` HEAD `59bbeee`. *(This field read "**No merge commit — nothing is merged.** `phase-4` untouched at `f39fa6c`" until closure.)* All ten commits above **remain reachable from `phase-4`** — which is why master scope §16's close-out sequence item 6 requires a merge commit rather than a squash: this record cites individual SHAs. `main` untouched at `7e273e6`. **No `phase-4f` branch exists** |
| 7 | Phase status | **COMPLETE, VERIFIED, MERGED AND CLOSED; final gate verdict GO** (2026-09-15, Gate Review §15 and §19). Merged into `phase-4` as `59bbeee`; resulting `phase-4` HEAD `59bbeee3838290ab31b0627b64c6fc0b2bceb13b`. ***Superseded text, true at head `df4606c`:*** **COMPLETE AND VERIFIED, NOT MERGED; gate verdict GO.** ***End superseded text.*** **AC-6 is MET at four tiers** — unit, integration, real-Postgres and browser. CI is **37/37 green at the exact head SHA `a044782`**, zero failed and zero cancelled. **No conditions are attached to this GO.** Three findings remain OPEN as explicitly non-blocking carry-forwards (field 21), and **CF-9, CF-10 and CF-11 remain OPEN** |
| 8 | Production SLOC | **35,733** on the scope comparable to Phase 4B's, 4C.2's and 4D's (`services/*/src` + `packages/*/src` + `services/*/alembic/versions`); **41,074** on 4C.2's wider scope (adding `agent-os/*/src`, `agent-os/*/alembic/versions`, `agents/*`); **44,706** on the full scope also including `apps/*/src`. Measured at head `a044782` from pristine `git archive` extracts. Growth is reported against the branch base measured with **identical scope strings**: base `f39fa6c` gives **34,469 / 39,810 / 43,114**, which **reproduces Phase 4D's corrected closing values exactly** (4D Gate Review §22.6) — that is what makes the series continuous. Deltas: **+1,264 / +1,264 / +1,592**. The comparable and wider scopes move identically because 4E's engine code lands entirely in `services/`; the wider gap on the third scope (+328) is the panel and its entity module under `apps/`. **Neither SLOC milestone (30,000 / 50,000) is crossed — 5,294 to spare on the widest scope** |
| 9 | Total SLOC | **Not reported.** This review reports production scopes and the metric set, not a whole-repository total. Not backfilled |
| 10 | SLOC methodology/tool | **`cloc` v2.06**, `--skip-uniqueness --quiet`, measured from pristine `git archive` extracts — the tool, version *and* the extraction discipline Phase 4D's Gate Review §22.6 corrected to, so this figure continues that series comparably rather than only directionally. The **`scc` series (2D-B → 2D-C) remains separate and non-comparable**, and the Option A / Option B methodology decision in [`project-health-master.md`](project-health-master.md) §2 **remains open and is not decided here** |
| 11 | SLOC scope | Three scopes reported side by side, continuing 4C.2's and 4D's shape. **§2 needs no new methodology entry** — neither the tool nor the scope definitions changed. Both revisions were measured with identical scope strings, so the deltas are comparisons rather than estimates. TDD §17's table was measured at `407457d`; re-measurement at `a044782` gives **identical** figures, because the three intervening commits touch documentation, workflows, compose, tests and one **comment-only** change to `api-gateway`'s `routing.py` (cloc's comment column moves +23, its code column does not move) |
| 12 | Test status | **Local and CI, both green.** Local at `a044782`: `npx turbo run test --force` **31/31, `Cached: 0`, 51.099 s** — **2,614 passed, 384 deselected, 0 failed** — plus **283** `tools/tests`, for **2,897 passing, 0 failing**. `npx turbo run lint` **31/31**; `typecheck build` **34/34**; `uv run lint-imports` **7 kept, 0 broken**; codegen **116 TypeScript files, zero drift** (regenerated, working tree unchanged afterwards). CI at `a044782`: the `checks` job — which runs that same battery plus `docker compose config --quiet` — **success**. **All ten of TDD §14.5's negative controls are demonstrated**, two of them by the type system rather than only by a test, and **ten mutations** each failed a named test |
| 13 | Test count | `digital-twin-engine` **161 collected** — 143 default tier + **18 `real_infra`, all 18 passing in CI**. `memory-engine` **130 collected** — 126 default + **4 `real_infra`, all 4 passing in CI**; that tier is **new in 4E** and is the point of it. `apps/web-client` **217** vitest. `tools/tests` **283** (up from 246: the two-hop rule and its four anti-vacuity controls). **Playwright: 18 specs — 17 passed, 1 skipped, 0 failed (55.1 s)**, of which **3 are AC-6's and all 3 passed in a real browser** |
| 14 | Coverage | `nova_digital_twin_engine.domain` **100%** (357 statements, **0** missed) against the 85% `fail_under` gate. `api-gateway`, `memory-engine`'s repository layer and `apps/web-client` are not coverage-gated — the gate is `[tool.coverage.report]` in `pyproject.toml` and applies to Python domain packages only |
| 15 | CI status | **FULLY GREEN — 37 of 37 check runs `success` at head `a044782`**, zero failed and zero cancelled (2026-09-15). PR Checks **#102** (run [`34954932427`](https://github.com/Tudor191/NOVA-PROJECT-BIBLE/actions/runs/34954932427)) — `checks` and `Playwright golden path` both success. Real-Infrastructure Checks **#140** (run [`34954932413`](https://github.com/Tudor191/NOVA-PROJECT-BIBLE/actions/runs/34954932413)) — **14/14**. Build & Scan **#102** (run [`34954932430`](https://github.com/Tudor191/NOVA-PROJECT-BIBLE/actions/runs/34954932430)) — **20 images + `dependency-audit` = 21/21**, every image recording Build success and Scan success with no fail-fast cancellation. **This satisfies protocol §3.2 condition 7.** The Playwright job's own log line reads `golden path @ a044782360591e1d064e6dc053ce6926fc32c3e2`, so the evidence is anchored to the exact head rather than inferred from it. **Re-confirmed at the documentation head `ebef196915b271987144a288840f58c609a55ede`: 37/37 `success` again**, zero failed and zero cancelled — PR Checks #103 ([`34958056545`](https://github.com/Tudor191/NOVA-PROJECT-BIBLE/actions/runs/34958056545)), Real-Infrastructure Checks #141 ([`34958056512`](https://github.com/Tudor191/NOVA-PROJECT-BIBLE/actions/runs/34958056512)), Build & Scan #103 ([`34958056522`](https://github.com/Tudor191/NOVA-PROJECT-BIBLE/actions/runs/34958056522)). `git diff a044782..ebef196 -- ':!docs'` is **empty** (Gate Review §18) |
| 16 | Real-infrastructure status | **PASSED, in CI.** `real-infra (digital-twin-engine)` **18 passed, 143 deselected, 10.47 s**; `real-infra (memory-engine)` **4 passed, 126 deselected, 15.56 s**. *(The pre-Gate record cited 13.61 s and 15.83 s for these two; the Gate Review re-read the job logs and carries the corrected durations. The pass and deselect counts were correct.)* The `memory-engine` tier is **new in 4E** and earned its place immediately: `memory-engine` had **no real-Postgres coverage at all**, which is exactly how `PostgresMemoryRepository.create_long_term` shipped discarding `created_at` — every tier above the real driver saw a plausible timestamp because the column default filled it in. The named test is `test_a_historical_created_at_is_persisted_not_overwritten_by_the_column_default`. **Docker was unreachable locally throughout, so every real-infrastructure and browser result in this record is CI's and is labelled as such** |
| 17 | Documentation health | Two new documents (this record and the Gate Review), one corrected living reference (`ENGINEERING_ROADMAP.md`'s 4E row, which read **"Not started"** — a direct contradiction of the repository, corrected additively with the original cell text preserved), one master-index row, and one dated additive note on PR #29's body. **No historical section rewritten.** TDD 4E was already current: §0.1's four findings, §19's and §20's ratified decisions and §17's SLOC table all hold at `a044782`, and re-measurement confirmed §17's figures rather than requiring an edit. **`README.md` still carries no Phase 4 status line** — a Phase-4-level obligation recorded in Phase 4C's health record and re-stated by 4D §13.2, whose `definition-of-done.md` item-5 triggers 4E does not fire (Gate Review §14.3, as superseded in part by §19.1: the "is not merged" clause no longer holds, the conclusion does, and the gap is inherited by Phase 4's closure exactly as 4D left it). **The 2026-09-15 closure pass adds five more documents**: Gate Review §19, this record's merged-state fields, the master-index row and note, the roadmap row, and the master scope's 4E closure note — all additive, every superseded value preserved inline and marked |
| 18 | Architecture status | `digital-twin-engine` **extended, not rewritten**: nine new domains, a pure `domain/derivation.py` rule table, and three additive tables in migration `0003` (`domain_model`, `domain_evidence`, `project_model`) with **0 existing tables altered**. `domain_evidence` carries a composite foreign key into `domain_model` with `ON DELETE CASCADE`, so provenance cannot exist for a domain that was never derived, and its primary key `(user_id, domain, source_record_id)` makes at-least-once redelivery a no-op rather than an inflated count. One `api-gateway` `UpstreamRoute` (`/v1/digital-twin`, forwarded 1:1 per D-6), five REST routes, one lazily-loaded panel chunk (6.61 kB). **One new compose service, `memory-engine-worker`** — an existing arq worker finally deployed, not new code; `services/memory-engine/src/nova_memory_engine/workers/` is **byte-identical to the base**. `memory-engine` gains two lines in its repository insert and one field on an existing event payload; **no migration** |
| 19 | Security status | **Nothing weakened; one control strengthened and one surface disclosed.** `PUBLIC_TOPICS` **byte-identical** — `git diff f39fa6c..a044782 -- services/ws-gateway/` is **0 lines**. **0 new Event Bus subjects**: 118 registered subjects at base and 118 at head, with `@register_payload` call sites at 119 on both. **0 `autonomy.*` subjects** anywhere. **No `TrustMetric` surface change** — no `+`/`−` line in the whole diff touches it, and `autonomy-engine`'s trust adapter is sha256-pinned by `test_control_9d`. **No engine-to-engine HTTP** — zero HTTP-client imports in `digital-twin-engine/src`; the ADR-004 violation the TDD itself specified was found and resolved to the Event Bus (§0.1.5). **No duplicated approval state** — zero occurrences of `approval` in the engine's source. **No browser-to-NATS path** — the realtime client admits only `ws:`/`wss:`. Privacy is **fail-closed**: a `PUBLIC`/`INTERNAL` allow-list, stricter than the ratified rule, because `PrivacyLevel` has no `PRIVATE` member (§0.1.6); asserted at five tiers including the browser. ADR-025 preserved — 4E's five routes resolve `primary_user_id` server-side and take no `user_id`. **0 new dependencies.** Trivy: **21/21 green**. **One surface is newly reachable and disclosed, not fixed** — see field 21, item (2) (TDD §0.1.7) |
| 20 | Unverified infrastructure | **Nothing specific to 4E.** Both tiers that could not run locally ran in CI: the 22 `real_infra` tests and all 18 Playwright specs, against the real Docker stack. The compose service, the migration entry and both CI matrix additions started for real — `nova-local-memory-engine-worker-1` and `nova-local-digital-twin-engine-1` both appear in the e2e stack's own teardown log at `a044782`. **Still unverified, carried forward unchanged:** Prometheus scrapes only `nova-core`. The one Playwright skip is **4B's** `approval-lifecycle`, which skips when the Critical action is denied at `action-engine`'s identity-confidence gate — CF-9's fail-closed default working as designed, pre-existing, and not 4E's |
| 21 | Open blockers | **NONE. The GO carries no conditions.** Three findings remain **OPEN** as explicitly non-blocking carry-forwards, each disclosed and none affecting an acceptance criterion: **(1) `nova_testkit` Postgres image drift** — `_POSTGRES_IMAGE = "postgres:16-alpine"` while the compose file uses `pgvector/pgvector:pg16`, and two engines' migration `0001` needs `CREATE EXTENSION vector`; pre-existing, not 4E's, excluded from the approved fix scope (TDD §0.1.8). **(2) The D-6 prefix exposes six pre-4E operations that take a caller-supplied `user_id`**, three of them writes (`PATCH /profile`, `PATCH /proactive-policy`, `POST /reset`); not a confidentiality vector under ADR-025, but an integrity surface and a latent multi-user hazard; **reported, not fixed**, because changing 2D-D's shipped routes would desynchronise them from an RPC that carries `user_id` by design — pinned on both sides by two named tests (TDD §0.1.7). **(3) The stranded-outbox guard omits the `memory` schema** — new, minor, one-line remedy known; not fixed because the approved scope for `a044782` was exactly three files. **CF-9, CF-10 and CF-11 all remain OPEN**: CF-9 by ratified decision D-4D-2 and untouched here; CF-10 asserted as five separate unresolved properties by `test_control_9a…9e` per ratified §19.2; CF-11 has no producer added by 4E. **AC-4 clauses 2 and 3** stay deferred by the user approval of 2026-09-07, untouched. One **category-13** item exists — item (2) above, TDD §0.1.7, names a decision the user may wish to take — and it **does not require a decision for this gate**, so protocol §3.2 condition 11 holds |
| 22 | Branch hygiene | **`phase-4e` preserved at `df4606c12628d3097645624103ead4566f4783af`, pushed, not deleted, and a git ancestor of `phase-4`** — the 4C.2 and 4D precedent. Last code-bearing head `a044782360591e1d064e6dc053ce6926fc32c3e2`; the two commits above it are documentation only. *(This field read "**`phase-4e` pushed, not merged, not deleted** … current head `ebef196`. Nine commits" until the second documentation commit and the 2026-09-15 merge.)* Ten commits on the branch, **linear, zero merge commits**; the only merge commit is `59bbeee` on `phase-4` itself; **no force push, no rebase, no squash, no history rewrite** at any point. `phase-4` untouched at `f39fa6c`; `main` untouched at `7e273e6`. **No `phase-4f` branch exists.** Remote branch deletion remains blocked by the organization egress policy master scope §16 records, so branches accumulate by design |
| 23 | Notes / important findings | **The two controls that matter most are types, not tests.** `DomainModel`'s two validators make *"populated with zero evidence rows"* — the single claim Part 16 §69 forbids — and *"a non-populated state with no machine-readable reason"* **unconstructible**, and enforce the ratified per-domain floor (`Software`/`Hardware Environment` cannot represent anything but `empty`/`unavailable`; `Knowledge`/`Skill Profile` cannot claim `populated`). A derivation bug raises rather than rendering a plausible number. **AC-6's "simulated multi-week gap" is simulated in the data, never in the clock** — real `created_at` column values 118 to 37 days in the past, through the real repository into real PostgreSQL, on the real transactional outbox, over the pre-existing `memory.long_term.created` subject; a repository-wide sweep for `freeze_time`, `freezegun`, `time_machine` and `FakeClock` returns **zero matches**, and `derive_project_models` reads no clock at all (`now` is a caller-supplied parameter). **The Playwright gap assertion parses a number rather than matching words**, because a string match would pass equally on *"0 days since last activity"* — the one answer that would mean the mechanism had failed. **Three precision failures are recorded rather than buried**, because they are the failure mode controls are prone to: a no-polling check that first fired on the module's own docstring, `derive_domain`'s empty branch omitting `unavailable_fields`, and a two-hop rule whose first draft over-fired on RPC `*.request` subjects that never touch an outbox. **The deployment finding is the milestone's most consequential one:** because `memory.long_term.created` is not public, Phase 4A's stack guard exempted `memory-engine` from needing a worker, no compose service ran it, and therefore **no `memory.*` subject had ever been published in the local stack** — invisible until AC-6 needed it. Fixed by deploying the existing worker and by teaching the guard the two-hop shape **structurally**, with four anti-vacuity controls proving the new rule is live |
