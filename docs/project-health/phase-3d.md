# Phase 3D — Project Health Snapshot

Source: this session's own direct, live implementation and verification of `action-engine` plus capability-engine's additive `capability.resolve.request` contract extension, against the approved research/plan (`docs/design/phase-3/13-3d-action-engine-research.md`, PR #12) and the finalized TDD 3D (`docs/design/phase-3/07-tdd-3d-action-engine.md`).

| # | Field | Value |
|---|---|---|
| 1 | Phase identity | Phase 3D |
| 2 | Phase/Sub-Phase name | `action-engine` (Action Principle lifecycle, approval loop, ADR-032 identity-confidence gate, `CapabilityPort` consumer) plus the additive `capability.resolve.request` `name` field on `capability-engine` |
| 3 | Report date | 2026-08-17 |
| 4 | Branch | `phase-3d-action-engine`, based directly on `phase-3b-planning-domain` (sibling of the docs-only `phase-3d-research` branch/PR #12, not stacked on it — same discipline as Phase 3C's `phase-3c-research`/`phase-3c-capability-engine` sibling pair) |
| 5 | PR number | See final chat report for this session (opened after this snapshot was written; not yet merged) |
| 6 | Commit / merge commit | See final chat report for this session's exact head SHA |
| 7 | Phase status | Implementation complete, locally verified (lint/mypy/tests/coverage/import-linter/docker-compose config/TypeScript codegen), **not yet merged** — per explicit standing instruction, this PR is not to be merged in this session. |
| 8 | Production SLOC | Not measured in this snapshot (no SLOC tooling run this session; consistent with the Phase 2D-D/3A/3B/3C-established gap already tracked in `project-health-master.md` §4, "ACTIVE BUT NOT REPORTED") |
| 9 | Total SLOC | Not measured in this snapshot |
| 10 | SLOC methodology/tool | Not run this session |
| 11 | SLOC scope | Not applicable |
| 12 | Test status | `action-engine` (`-m "not real_infra"`): 47/47 passed, 8 deselected (`@pytest.mark.real_infra`). `capability-engine` (unaffected by the additive contract extension, re-verified for regression): 61/61 passed, 6 deselected. `nova-contracts`: 86/86 passed. Full workspace (`pnpm turbo run test --force`, all 22 packages): 22/22 successful. |
| 13 | Test count | `action-engine`: unit (`test_pipeline.py` — 15, `test_risk_classification.py` — 7), contract (`test_action_payloads.py` — 10), integration (`test_events_action_execute.py`, `test_approvals_api.py`, `test_capability_client.py`, `test_communication_client.py`, `test_identity_client.py`, `test_health.py`) — 47 selected total, plus 8 real-infra Postgres tests deferred to CI (`test_repository_real_postgres.py`, all 8 marked `real_infra`, matching the 8-deselected count reported above). |
| 14 | Coverage | `action-engine` domain: 97% (`pipeline.py` 95%, `models.py`/`ports.py`/`risk.py` 100%), vs. the 85% gate. `capability-engine` domain: unchanged at 97% (confirms the additive contract change introduced no regression). |
| 15 | CI status | See final chat report for this session — the real GitHub Actions run against the opened PR |
| 16 | Real-infrastructure status | `tests/integration/test_repository_real_postgres.py` (new, action-engine): real-Postgres round trips for all four tables (`action`, `pending_approval`, `action_execution_history`, `identity_confidence_policy`), including the `Action.id` primary-key uniqueness → `ActionAlreadyExistsError` translation the natural-key idempotency guard (§5.3) depends on. **Not executed in the environment this file was written in** (no reachable Docker daemon here, same standing limitation documented in every prior real-infra test in this codebase) — added to `real-infra-checks.yml`'s matrix for CI execution; see final chat report for the actual CI result. |
| 17 | Documentation health | Two additive correction notes already landed on `06-tdd-3c-capability-engine.md` (§4, §11) via PR #12, reconciling the stale "not yet fixed" language against the actually-shipped RPCs — not re-touched here. This implementation introduces no new documentation drift: `README.md` for `action-engine` was written to match the finalized architecture exactly (approved decisions §5.1/§5.2/§5.3, Fork 3C-3, Fork E2), mirroring `capability-engine`'s own `README.md` structure. |
| 18 | Architecture status | Approved decision §5.1 (execution_target = capability name) implemented via `CapabilityResolveRequestPayload`'s additive `name` field, `capability-engine`'s `find_by_name` repository method, and `main.py`'s branching resolve handler. Approved decision §5.2 (stage-2 structural validation, stage-5 deep validation) implemented — deep `Capability.input_schema` validation against resolved parameters is disclosed as not yet implemented (no document specifies the exact validation mechanism beyond the field's own presence; see `domain/pipeline.py`'s own stage-5 comments) and is a candidate follow-up, not silently skipped. Approved decision §5.3 (natural-key idempotency on `Action.id`) implemented, mirroring Fork 3C-4 exactly, including the concurrent-retry-race path (`ActionAlreadyExistsError` → re-check → "already in flight"). Fork 3C-3 (rollback ownership, Option B) implemented — `action-engine` owns rollback entirely via read-before-write against the unmodified `capability.invoke.request` RPC, `restore_file` only. Fork E2 (Phase-3-owned `action.approval.*` namespace) implemented and tested (`test_fork_e2_namespace_boundary_never_uses_autonomy_prefix`). import-linter 6/6 contracts kept; `nova_action_engine` registered in `root_packages`, contracts 1-3 (auto), contract 4/ADR-020 (manual, added this session), contract 6/ADR-034 (already present). |
| 19 | Security status | ADR-032 identity-confidence gate implemented at stage 3: absent policy or absent/timed-out confidence signal fails closed (treated as zero confidence against a maximum-confidence-required default), tested explicitly (`test_adr032_gate_fails_closed_on_absent_confidence_signal`, plus an end-to-end RPC-level test in `test_events_action_execute.py`). `action-engine` never performs identity recognition itself — reads only `world-model-engine`'s existing `present_identities` field. The approval loop's timeout path denies, never auto-approves (tested: `test_critical_risk_action_timeout_denies_never_auto_approves`). |
| 20 | Unverified infrastructure | Docker build for `services/action-engine/Dockerfile` and the real-Postgres repository tests are unverified locally (no reachable Docker daemon in this implementation environment) — the same standing, disclosed limitation as every prior Phase 3 engine in this codebase. Resolution path: real GitHub Actions evidence, per the same precedent Phase 3C used; see final chat report. |
| 21 | Open blockers | None identified for the implementation itself. Pending: real GitHub Actions CI confirmation (field 15/16); the deep `Capability.input_schema` parameter-validation mechanism (field 18) remains a disclosed, non-blocking gap — no document specifies it, and TDD 3D's own acceptance criteria do not name a concrete schema-validation library/approach. |
| 22 | Branch hygiene | `phase-3d-action-engine` is new this session, not yet merged, not a candidate for deletion. `phase-3d-research` (PR #12, docs-only) remains open and unmerged, unaffected by this branch. |
| 23 | Notes / important findings | One factual correction to PR #12's own §5.4 surfaced during implementation: `communication-engine`'s `addressee_fusion.py` already consumes `present_identities` (for addressee detection, not ADR-032 authorization) — `action-engine` is not the first-ever consumer of that field, contradicting PR #12's "first-ever consumer" framing. Judged a minor factual correction, not an architectural conflict (no approved decision depends on the "first consumer" framing); disclosed in the final chat report's "deviations from PR #12" section rather than treated as a blocker. |
