# Phase 3D — Project Health Snapshot

Source: direct, live implementation and verification of `action-engine` plus
capability-engine's additive `capability.resolve.request` contract extension,
against the approved research/plan (`docs/design/phase-3/13-3d-action-engine-research.md`,
originally PR #12, synced into canonical lineage 2026-08-19 via PR #15) and the finalized TDD 3D
(`docs/design/phase-3/07-tdd-3d-action-engine.md`), further corrected by the
Phase 3D documentation and project-state synchronization pass (2026-08-18) —
see the [Phase 3D Gate Review](../roadmap/architecture-reviews/phase-3d-action-engine-gate-review.md)
for the full analysis behind fields 18 and 21 below.

| # | Field | Value |
|---|---|---|
| 1 | Phase identity | Phase 3D |
| 2 | Phase/Sub-Phase name | `action-engine` (Action Principle lifecycle, approval loop, ADR-032 identity-confidence gate, `CapabilityPort` consumer) plus the additive `capability.resolve.request` `name` field on `capability-engine` |
| 3 | Report date | 2026-08-17, corrected 2026-08-18 (documentation pass), updated again 2026-08-18 (closure pass, deep validation implemented), updated a third time 2026-08-18 (post-merge sync, PR #13 merged into `phase-3b-planning-domain`), updated a fourth time 2026-08-19 (PR #12 research sync via PR #15 merged) |
| 4 | Branch | `phase-3d-action-engine`, based directly on `phase-3b-planning-domain` (sibling of the docs-only `phase-3d-research` branch/PR #12, not stacked on it — same discipline as Phase 3C's `phase-3c-research`/`phase-3c-capability-engine` sibling pair) |
| 5 | PR number | PR #13, `Tudor191/NOVA-PROJECT-BIBLE`, opened against `phase-3b-planning-domain`, **merged 2026-08-18** as squash commit `ac285bc3533fb24d0434d7675b8fc3af2db1d079` |
| 6 | Commit / merge commit | Head `046d459fb5e36416b57c0a378d7ef84fc49aa933` (4 commits: `21b4b07` implementation, `503a331` a real-Postgres-CI-caught bugfix, `4db1294` a project-health documentation update, `046d459` stage-5 deep `Capability.input_schema` validation — see field 16/18), squash-merged into `phase-3b-planning-domain` as commit `ac285bc3533fb24d0434d7675b8fc3af2db1d079` (2026-08-18) |
| 7 | Phase status | **Implementation complete against all 7 of TDD 3D's own §14 acceptance criteria**, locally verified (lint/mypy/tests/coverage/import-linter/docker-compose config/TypeScript codegen), CI green (field 15); **merged into `phase-3b-planning-domain` 2026-08-18** (squash commit `ac285bc3533fb24d0434d7675b8fc3af2db1d079`), per the user's explicit merge approval. Criterion 7 (deep `Capability.input_schema` validation, added by the 2026-08-18 documentation pass, closed the same day by commit `046d459`) is now met — see field 18. No open acceptance-criteria gap remains. |
| 8 | Production SLOC | Not measured in this snapshot (no SLOC tooling run this session; consistent with the Phase 2D-D/3A/3B/3C-established gap already tracked in `project-health-master.md` §4, "ACTIVE BUT NOT REPORTED") |
| 9 | Total SLOC | Not measured in this snapshot |
| 10 | SLOC methodology/tool | Not run this session |
| 11 | SLOC scope | Not applicable |
| 12 | Test status | `action-engine` (`-m "not real_infra"`): **62/62 passed**, 8 deselected (`@pytest.mark.real_infra`) — 47 original + 15 new for stage-5 deep validation (commit `046d459`). `capability-engine` (unaffected, re-verified for regression): 61/61 passed, 6 deselected. `nova-contracts`: 86/86 passed. Full workspace (`pnpm turbo run test --force`, all 22 packages): 22/22 successful. |
| 13 | Test count | `action-engine`: unit (`test_pipeline.py` — 19 [15 original + 4 new deep-validation pipeline tests], `test_risk_classification.py` — 7, `test_parameter_validation.py` — 11 new), contract (`test_action_payloads.py` — 10), integration (`test_events_action_execute.py`, `test_approvals_api.py`, `test_capability_client.py`, `test_communication_client.py`, `test_identity_client.py`, `test_health.py`) — 62 selected total, plus 8 real-infra Postgres tests deferred to CI (`test_repository_real_postgres.py`, all 8 marked `real_infra`, matching the 8-deselected count reported above). Deep `Capability.input_schema` validation at stage 5 is now covered: 11 unit tests (valid/invalid parameters, missing required fields, wrong types, nested structures, array items, malformed schemas) plus 4 pipeline-level tests proving the schema actually enforced is the one `capability_port.resolve()` returns. |
| 14 | Coverage | `action-engine` domain: 97% (`pipeline.py` 95%, `parameter_validation.py` 100%, `models.py`/`ports.py`/`risk.py` 100%), vs. the 85% gate. `capability-engine` domain: unchanged at 97% (confirms no regression). |
| 15 | CI status | **22/22 checks green on real GitHub Actions**, confirmed live at head `046d459` (2026-08-18: `build-and-scan` × 12 including `action-engine`'s own build+Trivy scan and `capability-engine`'s regression build, `dependency-audit`, `checks` (pr-checks), `real-infra` × 8 including `action-engine`'s own real-Postgres job). Reached green after a fix-forward commit (`503a331`) — the first push (`21b4b07`) failed `real-infra (action-engine, ...)` with 2 real bugs the local environment's absent Docker daemon could never have caught; see field 16. The subsequent `4db1294` (docs-only) and `046d459` (deep-validation feature) commits each re-ran the full 22-check matrix green at their respective new heads. |
| 16 | Real-infrastructure status | **Fully real-infra confirmed via CI.** `tests/integration/test_repository_real_postgres.py` (8 tests) ran for real against a throwaway `postgres:16-alpine` testcontainer and passed all 8 after one fix-forward commit. The first CI run caught two genuine bugs invisible to local verification (no Docker daemon in the implementation environment): (1) `PostgresActionRepository.get_result()` had an operator-precedence bug (`x if cond else y, z` binds as a tuple, not `(x if cond else y), z`) that made it always return a 2-tuple, never `None`, contradicting its own documented "returns `None` if no result recorded yet" contract; (2) the pending-approval round-trip test inserted a `pending_approval` row without its FK-required parent `action` row first (production code never does this — `insert_pending_approval` always follows `insert(action)` in the approval loop). Both fixed in commit `503a331`; CI re-ran green. `services/action-engine/Dockerfile` also built and Trivy-scanned for real by CI, clean. |
| 17 | Documentation health | Two additive correction notes landed on `06-tdd-3c-capability-engine.md` (§4, §11): PR #11's earlier sync of `phase-3c-research`'s own decisions, and PR #12's own correction (RPC-precision note), the latter synced into canonical 2026-08-19 via PR #15. This implementation introduces no new documentation drift beyond field 18/21's disclosed gap: `README.md` for `action-engine` was written to match the finalized architecture exactly (approved decisions §5.1/§5.2/§5.3, Fork 3C-3, Fork E2), mirroring `capability-engine`'s own `README.md` structure. TDD 3D itself now carries an additive §14 criterion 7 (2026-08-18) documenting the deep-validation gap in the canonical lineage. **Update (2026-08-19):** PR #12 itself (`docs/design/phase-3/13-3d-action-engine-research.md`) is now merged into canonical lineage via PR #15, with its own sync note reconciling its 9-item acceptance-criteria list against TDD 3D §14's separate 7-item list. |
| 18 | Architecture status | Approved decision §5.1 (execution_target = capability name) implemented via `CapabilityResolveRequestPayload`'s additive `name` field, `capability-engine`'s `find_by_name` repository method, and `main.py`'s branching resolve handler. **Approved decision §5.2 (stage-2 structural validation, stage-5 deep validation) is now fully implemented (closed 2026-08-18, commit `046d459`).** The 2026-08-17 documentation pass found the gap (stage 5 performed no schema validation, contradicting the prior "candidate follow-up" characterization); the user directed it closed. `domain/parameter_validation.py` (new, pure, framework-free, 100% covered) validates `Action.parameters` against the resolved `Capability.input_schema` using the `jsonschema` library, against `input_schema`'s own existing JSON Schema vocabulary. Wired into stage 5 immediately after capability resolution/health-check, before stage 6 (Execute). A validation failure is `status="failed"`, never `"denied"`, per §5.2's own "Consequences." 15 new tests (11 unit, 4 pipeline-level). `jsonschema>=4.23` added as a dependency; a `[[tool.mypy.overrides]]` entry added to root `pyproject.toml` (no `py.typed` marker), matching the existing `asyncpg`/`pgvector`/`testcontainers` precedent. Approved decision §5.3 (natural-key idempotency on `Action.id`) implemented, mirroring Fork 3C-4 exactly, including the concurrent-retry-race path (`ActionAlreadyExistsError` → re-check → "already in flight"). Fork 3C-3 (rollback ownership, Option B) implemented — `action-engine` owns rollback entirely via read-before-write against the unmodified `capability.invoke.request` RPC, `restore_file` only. Fork E2 (Phase-3-owned `action.approval.*` namespace) implemented and tested (`test_fork_e2_namespace_boundary_never_uses_autonomy_prefix`). import-linter 6/6 contracts kept; `nova_action_engine` registered in `root_packages`, contracts 1-3 (auto), contract 4/ADR-020 (manual), contract 6/ADR-034 (already present). |
| 19 | Security status | ADR-032 identity-confidence gate implemented at stage 3: absent policy or absent/timed-out confidence signal fails closed (treated as zero confidence against a maximum-confidence-required default), tested explicitly (`test_adr032_gate_fails_closed_on_absent_confidence_signal`, plus an end-to-end RPC-level test in `test_events_action_execute.py`). `action-engine` never performs identity recognition itself — reads only `world-model-engine`'s existing `present_identities` field. The approval loop's timeout path denies, never auto-approves (tested: `test_critical_risk_action_timeout_denies_never_auto_approves`). Trivy: clean, confirmed by CI (field 15/16), unaffected by this documentation pass. |
| 20 | Unverified infrastructure | Docker build and the real-Postgres repository tests were "genuinely unverified locally" (no reachable Docker daemon in the implementation environment) — resolved via real GitHub Actions evidence instead, the same precedent Phase 3C used (`phase-3c.md` field 20). Nothing remains unverified in that sense as of this record — the remaining open item (field 21) is a missing feature, not an unverified one. |
| 21 | Open blockers | **None.** The one blocker this snapshot previously recorded — deep `Capability.input_schema` validation at stage 5 (field 18) — is closed (2026-08-18, commit `046d459`). All 7 of TDD 3D §14's acceptance criteria are now met. The SLOC methodology decision (`project-health-master.md` §2) predates and is unaffected by Phase 3D — still open, not a blocker. **Update (2026-08-19):** `phase-3d-research`/PR #12's sync into canonical lineage, previously deferred until PR #13 and PR #14 both merged, is now complete via PR #15 (merge commit `bf748d0ef4157da3a60e0eb880a24a58264d9387`). No open blockers remain for Phase 3D. |
| 22 | Branch hygiene | `phase-3d-action-engine` merged into `phase-3b-planning-domain` 2026-08-18 (squash commit `ac285bc3533fb24d0434d7675b8fc3af2db1d079`); the branch itself has not been deleted (no branch deletion authorized yet, per explicit standing instruction). `phase-3d-research` (PR #12, docs-only) merged into canonical lineage 2026-08-19 via PR #15 (merge commit `bf748d0ef4157da3a60e0eb880a24a58264d9387`); the `phase-3d-research` branch itself is now a full git ancestor of canonical and has not been deleted (no branch deletion authorized yet, per explicit standing instruction). `phase-project-health-documentation` (PR #9) and `phase-3-documentation-sync` (PR #11) have since merged into `phase-3b-planning-domain`; their branches were removed by GitHub's post-merge auto-delete, noted here for the record — no branch was deleted by direct request in violation of standing instruction. |
| 23 | Notes / important findings | One factual correction to PR #12's own §5.4 surfaced during implementation: `communication-engine`'s `addressee_fusion.py` already consumes `present_identities` (for addressee detection, not ADR-032 authorization) — `action-engine` is the first *authorization* consumer of that field, not the first consumer of the field itself. Judged a minor factual correction, not an architectural conflict (no approved decision depends on the "first consumer" framing). TDD 3D's own text (`07-tdd-3d-action-engine.md` §6) never made the "first consumer" claim in the first place — only PR #12's research document (§5.4) did — so no correction was needed in the canonical TDD for that specific claim; only the deep-validation gap (field 18) required an additive TDD note. Per the same sequencing precedent PR #11 established for `phase-3c-research` (research/decision documents get synced into canonical lineage only *after* their corresponding implementation PR is merged, not before), `phase-3d-research`/PR #12 was deliberately **not** merged into canonical lineage until both PR #13 (2026-08-18, squash commit `ac285bc3533fb24d0434d7675b8fc3af2db1d079`) and PR #14 (2026-08-18, merge commit `e9ea3b8c6ae99c645e6eb41b98fbe34c55f8ec39`) had merged. **Update (2026-08-19):** that sync is now complete via PR #15 (merge commit `bf748d0ef4157da3a60e0eb880a24a58264d9387`). |
